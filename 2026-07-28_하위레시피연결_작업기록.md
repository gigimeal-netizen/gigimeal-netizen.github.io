# 2026-07-28 재료 행 "다른 레시피 연결(하위 반죽)" 작업기록

## 배경

"초코바게트의 경우에는 어떻게 레시피를 작성해야할까? 바게트반죽을 만들어서 반죽의 일부(2kg)덜어 코코아파우더, 우유, 피칸을 넣어 다시 반죽을 하는 레시피야"라는 실사용 질문에서 시작했다.

지금까지의 "사전발효반죽(풀리시)" 기능은 방향이 반대다 — 미리 만들어서 최종 밀가루 100% 버킷에 포함되도록 본반죽에 섞어 넣는 것. 초코바게트는 그 반대로, **이미 완성된 반죽에서 일부를 떼어내 별도 재료를 더 넣는** 구조라 풀리시로 표현할 수 없었다.

처음엔 "바게트 반죽 2000g"을 초코바게트 레시피의 재료 행에 숫자로 그냥 입력하는 방법을 제안했는데, "원본 바게트 레시피가 바뀔 때마다 손으로 다시 계산해서 넣어야 해서 실무에서 곤란했다"는 피드백을 받아 실제 연결 기능을 만들었다.

## 구현

### 데이터 모델
- 재료 행(`state.ingredients[]`)에 `subRecipeId`(nullable) 필드 추가. 밀가루 행에는 추가하지 않음 — 밀가루는 실제 밀가루 타입이지 하위 반죽 개념이 아니라서.
- `flours`/`ingredients`가 Supabase `recipes` 테이블의 jsonb 컬럼이라 새 필드 추가에 스키마 마이그레이션이 필요 없다.

### 위치 — 레시피 편집 화면(계산기) 자체
재료 DB 연결(`ingredientId`, 원가 계산 전용)은 "레시피 작성을 방해하지 않기 위해" 일부러 원가 계산 모달로 분리했던 것과 정반대로, 이번엔 편집 화면 안에 뒀다 — 목적 자체가 "지금 이 레시피를 계산하면서 g수를 보는 것"이라 편집 화면 밖에 있으면 의미가 없기 때문.

- 재료 행 아래 `.ing-sub-link-row`: 연결 안 된 상태면 `.tag-toggle-btn` 스타일의 "+ 다른 레시피 연결(하위 반죽)" 텍스트 링크 하나만(태그 입력칸처럼 "선택 항목은 접어두기" 원칙). 누르면 재료 DB 연결과 같은 `.ing-link-combo`/`.combo-dropdown` 컴포넌트로 자기 소유 레시피(자기 자신 제외)를 검색해 고른다.
- 연결되면 "🔗 <레시피명>" 배지 + "구성 보기"(아코디언 토글) + "연결 해제" 버튼으로 바뀐다.
- 그 재료 행의 무게(g) 입력은 평소와 똑같다 — 특별한 입력 방식이 아니라 실제 그 재료가 몇 g 들어갔는지를 그대로 적는 것뿐이고, 연결은 그 g수 안에 뭐가 얼마나 들어있는지 보여주는 부가 정보.

### "구성 보기" — 스냅샷이 아니라 항상 최신 배합비 기준
`computeSubRecipeBreakdown(recipeId, targetWeight)`가 `computeCostBreakdown()`과 같은 비율 계산(제빵: 밀가루 100 기준 pct, 제과/기타: scale-1 저장 weight)을 재사용해서, 연결된 레시피를 그 재료 행의 "지금" 실제 무게로 스케일한 g수 목록을 계산한다. **매번 `myRecipes` 캐시에서 다시 계산** — 원본 레시피(바게트)의 배합비를 나중에 바꾸면, 그걸 연결해 쓰는 다른 레시피(초코바게트)의 "구성 보기"도 다음 갱신 때 자동으로 최신 비율을 반영한다. 이게 애초에 "숫자를 그냥 박아넣기"의 진짜 대안인 이유다.

`updateDisplays()`의 재료 루프(제빵/제과 양쪽)에서 `ing.subRecipeId`가 있으면 방금 계산한 그 행의 실제 무게로 `updateSubRecipeBreakdownEl()`을 매 갱신마다 호출 — 아코디언이 열려있든 닫혀있든 항상 최신 내용을 채워두고, 토글 버튼은 보이기/숨기기만 담당한다.

### 원가 계산에도 자동 반영 — 사실상 핵심 보너스
`computeCostBreakdown()`이 `row.subRecipeId`가 있으면 `myIngredients` 가격 대신 `costPerGramForRecipeId()`(조합 제품 기능이 이미 쓰던 함수, 그대로 재사용)로 그 레시피의 현재 g당원가를 가져온다. 이 함수가 내부적으로 `computeCostBreakdown()`을 다시 호출하므로, 하위 레시피가 또 다른 하위 레시피를 물고 있으면(다단계 연결) 원가는 재귀적으로 끝까지 계산된다.

**순환 참조 방지**: A가 B를, B가 다시 A를 하위 레시피로 물면 무한 재귀 → 스택 오버플로 가능성이 생겨서, `costPerGramForRecipeId()`에 계산 중인 recipeId를 표시하는 `costPerGramRecursionGuard`를 추가 — 순환이 감지되면 그 자리에서 "미가격" 처리하고 끊는다.

다만 "구성 보기" 화면 자체는 원가와 달리 **한 단계까지만** 펼쳐 보여준다 — 하위 레시피가 또 하위 레시피를 물고 있어도 그 안쪽까지 재귀적으로 펼치진 않는다(참고용 미리보기라 이 정도로 충분하다고 판단, 필요하면 나중에 확장).

## 알려진 제한사항

- **레시피 CSV 내보내기/가져오기는 이 필드를 다루지 않는다** — CSV 헤더가 이미 고정돼 있어서(`레시피명,종류,태그,모드,기준밀가루,배율,총반죽량,구분,재료명,퍼센트,무게`) 새 컬럼을 추가하는 건 별개의 더 큰 변경. CSV로 내보냈다 다시 가져온 레시피는 하위 레시피 연결이 빠진 채로 복원된다.
- `index.html`/`manager.html`은 로그인/레시피 목록 개념 자체가 없어서 이 기능은 `account.html`에만 있다.
- "구성 보기"는 한 단계까지만 펼쳐 보인다(위 참고) — 원가 계산은 재귀적으로 다 반영되지만 화면 표시는 아님.

## 검증

Node가 없는 환경이라 `uv run --with esprima python3`로 `<script>` 내용만 문법 검사(esprima, ES5 파서) — PARSE OK 확인. 실제 브라우저 동작 테스트는 아니므로, 실사용 중 이상이 있으면 알려달라고 안내 필요.

---

## 되돌림 (같은 날, 스크린샷 리뷰 후)

밀가루/재료 카드를 실제로 보니, **모든 재료 행마다** "+ 다른 레시피 연결(하위 반죽)" 텍스트 링크가 상시 노출돼 있어서 "다른레시피연결 하지 말자. 더 헷갈린다."는 피드백을 받았다. 기능 자체의 계산 로직 문제가 아니라, "거의 안 쓰일 옵션이 모든 재료 행에 항상 보이는" UI 배치 자체가 화면을 복잡하게 만든 것 — 이 앱이 여러 번 써온 "선택 항목은 접어두기"(태그 입력칸, 사전발효반죽 등) 원칙을 이번엔 지키지 못한 설계였다.

### 되돌린 범위 (흔적 없이 완전 롤백)

- `state.ingredients[]`의 `subRecipeId` 필드 (기본값 포함 모든 생성 지점)
- `buildIngRows()`의 `.ing-sub-link-row` 마크업 + `buildSubRecipeLinkUI()`/`attachSubRecipePicker()`/`updateSubRecipeBreakdownEl()` 세 함수
- `computeSubRecipeBreakdown()` 함수
- `computeCostBreakdown()`의 `row.subRecipeId` 원가 분기 (`ingredientId` 분기만 남김)
- `costPerGramForRecipeId()`의 순환 참조 감지 가드(`costPerGramRecursionGuard`) — 하위 레시피 원가 재귀 호출이 사라지면서 더 이상 필요 없어짐
- `applyRecipeRowToState()`의 두 `subRecipeId` 매핑
- 관련 CSS(`.ing-sub-link-row`/`.ing-sub-link-active`/`.ing-sub-link-badge`/`.ing-sub-breakdown`/`.sub-recipe-combo`/`.tag-toggle-btn.danger`)

`supabase/schema.sql`은 애초에 이 기능 때문에 바뀐 게 없었다(`flours`/`ingredients`가 jsonb라 필드 추가에 스키마 변경이 필요 없었던 것 — 되돌릴 때도 스키마 쪽은 손댈 게 없었다).

### 초코바게트 문제 자체는 아직 미해결

이번 시도로 배운 것: "재료 행마다 상시 노출되는 링크" 형태는 이미 한 번 시도했다가 UX 문제로 걷어낸 접근이다. 다음에 다시 다룰 때는 레시피 단위의 일회성 "가져오기"(복사, 실시간 연결 아님) 같은, 편집 화면을 상시 어지럽히지 않는 형태를 먼저 검토해볼 만하다.

### 검증

되돌린 뒤 `uv run --with esprima python3`로 `<script>` 문법 재검사 — PARSE OK. `grep`으로 `subRecipeId`/`ing-sub-link`/`attachSubRecipePicker`/`computeSubRecipeBreakdown`/`buildSubRecipeLinkUI`/`costPerGramRecursionGuard`/"하위 반죽" 전부 검색해 잔여 참조가 없는 것도 확인했다.
