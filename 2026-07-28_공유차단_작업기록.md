# 2026-07-28 공유 스팸 방지(받는 쪽 차단 기능) 작업기록

## 배경

레시피 아카이브를 커뮤니티에 공개하기 전에 준비가 됐는지 묻는 과정에서, 다음 두 가지 우려가 제기됐다.

1. "불특정 다수가 쓸 준비가 됐을까?"
2. "이메일로 공유하는 기능, 누가 막무가내로 특정 이메일에 필요없는 레시피를 왕창 보내서 쓰기 어렵게 만들면 큰일 아니냐 — 안 그래도 내 이메일이 고객센터(신고 내역 화면 접근 권한)에 이미 박혀있는데."

코드를 직접 확인한 결과 실제 취약점이었다:

- `share_recipe_with_email()`은 대상 이메일이 이미 이 앱에 가입된 계정이어야만 동작한다 — 아무 이메일에나 스팸을 보낼 수 있는 건 아니지만, 운영자 이메일(`gigimeal@gmail.com`)은 `is_report_admin()`에 이미 하드코딩돼 있어 가입이 확실한 표적이 된다.
- 호출 횟수 제한이 전혀 없다 — 레시피 하나 만들고 스크립트로 반복 호출하면 무한정 공유를 걸 수 있다.
- **결정적으로, 공유를 받는 쪽이 원치 않는 공유를 스스로 지울 방법이 없었다** — `recipe_shares`/`tag_account_shares` 둘 다 소유자(공유를 건 사람)만 행을 삭제할 수 있는 RLS 정책뿐이었다.

"순서대로 하자"는 요청에 따라, 이번엔 이 중 가장 시급한 3번(받는 쪽 방어권 없음)부터 해결했다. 나머지 두 가지(공유 요청 rate limit, 아카이브 신고/모더레이션 보강)는 다음 단계로 남겨뒀다.

## 설계 — 레시피 단위가 아니라 "소유자" 단위로 차단

레시피 하나씩 숨기는 방식은 공격자가 레시피를 계속 새로 만들어 재공유하면 매번 뚫린다. 그래서 "이 사람이 앞으로 거는 공유는 전부 막기"로 설계했다.

### 새 테이블/함수 (`supabase/schema.sql`)
- `share_blocks(blocker_id, blocked_owner_id, note, created_at)` — primary key는 두 id 조합, RLS는 `blocker_id = auth.uid()`인 행만 CRUD 가능.
- `is_blocked_by_recipient(p_owner_id)` — `security definer` 함수, "지금 로그인한 내가 이 owner_id를 차단했는가"를 검사.

### 이미 걸려있던 공유도 즉시 차단, 새 공유도 원천 봉쇄 — 양쪽 다
- `recipes` 테이블의 `"shared read access"` SELECT 정책에 `and not is_blocked_by_recipient(owner_id)` 조건을 추가 — 레시피 개별 공유든 태그 공유든 RLS 단계에서 걸러진다. `recipe_shares`/`tag_account_shares` 행 자체는 지우지 않으므로, **차단 해제 시 예전 공유가 자동으로 되살아난다.**
- `share_recipe_with_email()`/`share_tag_with_email()`에 차단 여부 검사 추가 — 차단된 상태면 예외를 던져 새 공유 생성을 막는다.
- 1회용 초대 링크 클레임(`claim_recipe_share_invite()`/`claim_tag_share_invite()`)에도 같은 검사를 추가하고 `'blocked'` 상태를 반환하게 했다 — **초대 링크가 이메일 공유 차단을 우회하는 구멍이 되지 않도록** 네 함수 모두 동일하게 처리.

## UI

- **"내 레시피" 목록**: 공유받은(내 소유가 아닌) 행에 삭제 버튼("×") 대신 차단 버튼("⦸")이 뜬다 — 소유 여부로 배타적으로 갈린다. 누르면 확인 후 `share_blocks`에 insert, 로컬 `myRecipes`에서 그 소유자의 모든 레시피를 즉시 제거.
- **"공유 관리" 화면 세 번째 탭 "차단 관리"**: 레시피/태그 탭과 같은 탭 전환 구조를 그대로 재사용(`switchShareTab('blocked')`). 차단 목록을 보여주고 "차단 해제" 버튼으로 되돌릴 수 있다.
- 차단된 소유자의 "신원 표시"는 완벽하지 않다 — `auth.users`를 클라이언트가 직접 조회할 수 없어서, 차단 시점에 열려있던 레시피 이름을 `note`에 스냅샷으로 저장해 "이 레시피를 공유했던 사용자"로만 식별한다(참고용).

## 기존 라이브 프로젝트 마이그레이션 필요

`supabase/schema.sql`을 통째로 재실행하는 게 아니라면, 이미 운영 중인 Supabase 프로젝트에는 다음을 SQL 에디터에서 직접 실행해야 한다:

1. `share_blocks` 테이블 생성 + RLS 정책
2. `is_blocked_by_recipient()` 함수 생성
3. `drop policy "shared read access" on recipes;` 후 새 조건(`and not is_blocked_by_recipient(owner_id)`)으로 재생성
4. `share_recipe_with_email`/`share_tag_with_email`/`claim_recipe_share_invite`/`claim_tag_share_invite` 네 함수를 `create or replace function`으로 재실행

## 알려진 제한사항 / 다음 단계 (1단계 — 차단 기능 시점)

- 공유 요청 자체에 대한 rate limit(짧은 시간에 공유를 몇 건 이상 걸면 막기)은 아직 없음 — 다음 단계.
- 레시피 아카이브(공개 게시판)의 신고/모더레이션 보강도 아직 — 다음 단계.
- 차단은 계정 공유(이메일 지정/초대 링크)에만 적용된다 — 링크 공유(`?share=`/`?tag_share=`, URL을 아는 사람 누구나)와 레시피 아카이브(공개 게시판)는 이 차단과 무관한 별개 경로.

---

## 2단계 — 공유/초대 링크 생성 rate limit (같은 날 이어서 진행)

1단계(차단)는 이미 걸린 공유를 받는 쪽이 사후에 정리하는 방어라, 상대가 차단하기도 전에 공격자가 순식간에 대량으로 공유를 걸어버리는 것 자체는 막지 못한다. "순서대로" 진행하기로 한 2번째 항목으로 사전 방어(rate limit)를 추가했다.

### 구현
- `share_rate_log(id, owner_id, created_at)` 테이블 — 클라이언트는 전혀 접근하지 않는다(RLS는 켜져 있지만 정책을 하나도 안 둬서 기본적으로 모든 비-슈퍼유저 접근이 막힌다). 오직 아래 `check_share_rate_limit()` 함수만 건드리며, 이 함수는 테이블을 만든 역할(스키마를 실행한 역할)로 실행되므로 RLS 대상이 아니다 — 이 파일의 다른 SECURITY DEFINER 함수들과 같은 원리.
- `check_share_rate_limit()` — **10분에 20건**(계정당)으로 제한. 호출될 때마다 (1) 1일 지난 오래된 기록을 지워서 별도 정리 작업 없이 테이블을 작게 유지하고, (2) 최근 10분 내 그 owner의 기록 수를 세어 20 이상이면 예외를 던지고, (3) 아니면 이번 시도를 기록하고 통과시킨다.
- **`share_recipe_with_email()`/`share_tag_with_email()`/`create_recipe_share_invite()`/`create_tag_share_invite()` 네 함수 모두 본문 맨 앞에서 이 함수를 호출** — 이메일이 실제로 가입돼 있는지 확인하기도 전에 rate limit부터 검사한다. 존재하지 않는 이메일로 실패하는 시도까지 횟수에 포함시켜서, "가입된 이메일이 뭔지 무제한으로 훑어보는" 것 자체도 같이 막기 위해서다.
- UI 쪽 변경은 없음 — 기존 모든 `supabase.rpc('share_recipe_with_email', ...)` 등 호출부가 이미 `res.error.message`를 그대로 alert하는 구조라, 새 예외 메시지("공유/초대 링크 요청이 너무 많아요 — 잠시 후 다시 시도해주세요")도 추가 코드 없이 그대로 노출된다.

### 알려진 트레이드오프
- "체크박스로 여러 레시피 골라 한 이메일에 일괄 공유" 기능(이전 세션에서 추가)은 레시피 하나당 `share_recipe_with_email()`을 한 번씩 호출한다 — 20개 넘게 한꺼번에 고르면 10분 한도에 걸려 일부만 실패할 수 있다. 정상적인 소규모 매장 사용 범위에서는 한 번에 20개 넘게 공유할 일이 거의 없다고 보고 받아들인 트레이드오프.

### 기존 라이브 프로젝트 마이그레이션

1단계 마이그레이션에 이어서 다음도 SQL 에디터에서 실행해야 한다:

1. `share_rate_log` 테이블 생성
2. `check_share_rate_limit()` 함수 생성
3. `share_recipe_with_email`/`share_tag_with_email`/`create_recipe_share_invite`/`create_tag_share_invite` 네 함수를 `create or replace function`으로 재실행

## 검증 (2단계)

`uv run --with esprima python3`로 `<script>` 문법 검사 — PARSE OK(2단계는 account.html을 건드리지 않아 이 검증은 1단계 변경에 대한 것). SQL 자체는 이 환경에 실행 가능한 Supabase 프로젝트가 없어 직접 실행 테스트는 못 했고, 기존 함수들과 동일한 패턴(security definer, exists 서브쿼리)을 그대로 따랐다.

---

## 3단계 — 레시피 아카이브 신고/게시 모더레이션 보강 (같은 날 이어서 진행, 마지막 단계)

"순서대로" 진행한 마지막 항목. 1·2단계는 계정 간 공유(이메일/태그/초대 링크)에 대한 방어였고, 이번엔 **레시피 아카이브(공개 게시판)** 자체 — 신고 시스템과 게시(공개 전환) 행위를 겨냥했다.

### 문제
- `recipe_reports`는 로그인 여부와 무관하게 누구나 insert할 수 있는 테이블이라(익명 요청은 `auth.uid()`가 없음) 앞서 쓴 신원 기반 rate limit(`check_share_rate_limit()`)을 그대로 못 쓴다.
- 아카이브 게시(레시피의 `is_public`을 true로 바꾸는 것)는 공유 RPC들과 달리 `account.html`이 `recipes` 테이블을 **직접 update**하는 경로라, 함수 호출부에 검사를 끼워 넣는 방식(RPC 진입점에 `perform check_...()`)을 쓸 수 없다.

### 구현
- **신고 품질 제약**: `recipe_reports_reason_not_blank`(빈 사유 금지), `recipe_reports_reason_maxlen`(500자 초과 금지) 체크 제약 추가. 클라이언트(`reportBtn` 핸들러)도 빈 사유를 먼저 걸러서 서버 제약 위반의 낯선 에러 문구 대신 "신고 사유를 입력해주세요" 안내를 보여주도록 수정.
- **레시피당 신고 누적 캡(50건)**: `enforce_recipe_report_cap()` — before insert 트리거. 신원과 무관하게 `recipe_id`별 개수만 세면 되므로 익명 요청에도 그대로 적용된다. 50건이면 이미 운영자가 검토하기 충분한 신호이고, 그 이상은 신호가 아니라 스팸이라고 판단해서 그 지점에서 추가 신고를 거부한다.
- **아카이브 게시 rate limit(계정당 10분에 10건)**: 함수 호출 방식 대신 **트리거**로 구현 — `enforce_archive_publish_rate_limit()`(before update on recipes)가 `is_public`이 false→true로 바뀌는 순간(새로 게시하는 순간)만 검사한다. 이미 공개인 레시피를 그냥 저장(다른 필드만 수정)하는 흔한 경우는 매번 다시 세지 않는다. `archive_publish_log(owner_id, created_at)`에 매 게시 시도를 기록 — `share_rate_log`와 같은 "정책 0개로 클라이언트 접근 차단, SECURITY DEFINER 함수/트리거만 사용" 패턴.
- 트리거 방식의 장점: 클라이언트가 어떤 경로로 update를 보내든(지금의 직접 update든, 나중에 생길 다른 경로든) 항상 걸린다 — RPC 하나에만 검사를 넣는 것보다 우회하기 어렵다.

### 의도적으로 하지 않은 것
- 신고 처리(비공개 전환/삭제) 시 신고 행을 그대로 삭제하는 기존 동작은 유지 — 처리 이력을 남기는 건 범위 밖으로 판단, 이 앱의 다른 삭제들과 같은 단순한 패턴을 유지했다.
- 콘텐츠 자체의 자동 필터링(욕설/스팸 텍스트 탐지)은 하지 않음 — 사람이 신고 내역을 보고 판단하는 현재 흐름에 계속 의존한다.

### 기존 라이브 프로젝트 마이그레이션

1·2단계에 이어서 다음도 SQL 에디터에서 실행해야 한다:

1. `recipe_reports`에 두 체크 제약 추가 (`alter table recipe_reports add constraint ...` 두 줄)
2. `enforce_recipe_report_cap()` 함수 생성 + `recipe_reports_cap` 트리거 생성
3. `archive_publish_log` 테이블 생성
4. `enforce_archive_publish_rate_limit()` 함수 생성 + `recipes_archive_publish_rate_limit` 트리거 생성

## 검증 (3단계)

`uv run --with esprima python3`로 `<script>` 문법 검사 — PARSE OK. 트리거를 실제 Postgres에서 실행해보지는 못했지만(이 환경에 연결 가능한 Supabase 프로젝트 없음), 표준 `before insert`/`before update` row-level 트리거 문법을 그대로 따랐다.
