# 2026-07-27 도움말 개편 + 접기/닫기 버그 두 건 작업기록

## 배경

두 가지를 동시에 지적받았다.

1. "계산기에서 사전발효반죽(풀리시) 추가 버튼 누르면 다시 닫을수가 없네." + "(풀리시) 문구 빼자"
2. "내 레시피에서도 태그로 필터링 버튼 누르면 다시 닫을수가 없네. 이게 괜찮은거야?"

## 원인 (두 버그가 같은 패턴)

둘 다 "펼치기" 버튼만 있고 "접기"가 없었던 게 아니라, **접는 시도 자체가 무효화되는 구조적 버그**였다:

- 사전발효반죽: `updatePoolishVisibility()`의 `show = isBread && (hasPoolish || poolishExpanded)` — 카드 안에 풀리시 행을 하나라도 넣으면 `hasPoolish`가 true가 되고, 이후로는 `poolishExpanded`를 false로 되돌려도 계속 떠 있는다.
- 내 레시피 태그 필터: `renderMyRecipesTagBar()`의 `expanded = myRecipesTagBarExpanded || myRecipesActiveTagFilter !== null` — 필터가 하나라도 걸려있으면 `myRecipesTagBarExpanded`만 false로 되돌려도 계속 펼쳐진 채로 남는다.

## 수정 — "접기 = 내용까지 정리하고 처음 상태로"

두 곳 모두 같은 원칙으로 수정했다.

- **사전발효반죽**: 카드 헤더에 `#closePoolishBtn`(× 버튼) 추가. 풀리시 행이 실제로 있으면 confirm 후 `stage==='pre'`인 밀가루/재료 행을 지우고 접는다(밀가루가 0개가 되면 기본 밀가루로 되돌림). 행이 없으면 확인 없이 바로 접힌다. 사용자에게 보이는 "(풀리시)" 괄호 문구는 `<h2>`와 "+ 사전발효반죽 추가" 버튼 두 곳에서 제거 — 내부 식별자(CSV 헤더, 변수명)는 안 건드림.
- **내 레시피 태그 필터**: `#myRecipesOrderBarRow`에 `#collapseMyRecipesTagBarBtn`("태그 필터 접기") 추가. 누르면 펼침 상태와 활성 필터를 **함께** 초기화하고 순서 편집 모드도 같이 끈다.

## 도움말 전면 개편

"현재 안쓰는 구조가 많다"는 지적으로 `#helpPage`를 검토한 결과, 2026-07-25 작성 이후 UI가 여러 번 바뀌면서(재료 관리/원가 계산이 계정 메뉴 → 하단 탭, 레시피 아카이브가 레시피 이름 옆 → 하단 탭) 실제로 틀린 정보가 된 문구들이 있었다.

- **아이콘 안내**: "계정" 설명에서 "재료 관리, 원가 계산" 언급 제거(이제 하단 탭). "레시피 아카이브 (레시피 이름 옆)" 항목 삭제 — 더 이상 그 자리에 없음. **"내 레시피" 아이콘 항목 신규 추가**(그동안 아예 빠져 있었음). **"화면 하단 탭" 소제목 아래 4개 탭(레시피/아카이브/재료/원가) 통째로 신규 추가** — 각 탭의 실제 SVG를 그대로 복사.
- **본문**: 사전발효반죽(풀리시), 태그 필터 칩과 순서 편집, 내 레시피의 삭제·순서 이동, 공유 관리의 일괄 이메일 공유·태그별 레시피 목록 표시 등 최근 추가된 기능들을 각 절에 채워 넣음.
- **"상황에 맞게 쓰기" 절 신설**(요청사항): "직원에게 특정 태그만 비공개로 공유하고 싶다면", "레시피 여러 개를 한 번에 공유하고 싶다면", "CSV로 백업하고 싶다면", "거래명세서로 재료 단가를 한 번에 갱신하고 싶다면", "레시피를 순서대로/태그로 정리하고 싶다면" 같은 시나리오별 안내를 추가.

## 검증

Node가 없는 환경이라 `uv run --with esprima`로 `<script>` 내용만 문법 검사(esprima, ES5 파서) — 실제 브라우저 동작 테스트는 아님.
