# 2026-07-27 퍼센트 저장 정밀도 버그 수정 작업기록

## 배경

`account.html`(풀리시/사전발효반죽 기능 사용 중, 밀가루 총량이 큰 배치)에서 사용자가 물 8750g, 소금 295g을 직접 입력했는데 화면에는 8751g, 296g으로 표시되는 문제가 스크린샷으로 보고됐다.

## 원인

`pctFromWeight(weight, flourWeight)`가 계산한 퍼센트를 `roundPct()`(소수점 2자리 반올림)로 곧바로 `state`에 저장했다. 총 반죽량이 큰 배치(예: 밀가루 총량 ~14,700g)에서는 2자리 정밀도로는 원래 입력한 정수 그램 값을 정확히 복원할 수 없다:

- 물 8750g → 8750/14700*100 ≈ 59.5057% → 2자리로 반올림 저장하면 59.51%
- 화면 갱신 시 무게를 다시 역산: 14700 × 0.5951 ≈ 8751.05g → 반올림 8751g

즉 "타이핑한 무게 → % 변환·저장 → 화면 갱신 때 다시 무게로 역산"하는 왕복 과정에서 %의 반올림 손실이 그램 단위 오차로 나타난 것. 총량이 작은 레시피에서는 오차가 반올림 경계 아래로 묻혀 거의 안 보이다가, 총량이 커질수록(산업용/대량 배치) 눈에 띄게 나타난다.

## 수정

`roundPct()`(2자리)는 화면 표시 전용으로 남기고, 저장용 `roundPctPrecise()`(6자리, `Math.round(p*1e6)/1e6`)를 새로 추가해서 역할을 분리:

- `pctFromWeight()`가 반환하는(=`state.flours[]`/`ingredients[].pct`에 저장되는) 값은 `roundPctPrecise()`
- 레시피 로드 시 DB/JSON → `state`로 매핑하는 지점(`applyRecipeToState()`, `account.html`의 `applyRecipeRowToState()`)도 `roundPctPrecise()`로 변경 — 안 그러면 저장했다가 다시 불러올 때 정밀도가 또 2자리로 깎여서 버그가 재발함
- `.pct-input`에 값을 채워 넣는 모든 지점(`buildFlourRows()`/`buildIngRows()`/`updateDisplays()`)은 그대로 `roundPct()` 유지 — 사용자에게는 여전히 "59.51%"처럼 깔끔하게 보임

**세 파일 모두 동일한 핵심 로직(베이커스 퍼센트 계산)을 공유**해서 `index.html`/`manager.html`/`account.html` 전부에 같은 수정을 적용했다. `manager.html`은 이 저장소의 `.gitignore`에 걸려 있어 git에는 안 잡히지만 디스크상 파일은 동일하게 수정됨.

## 검증

Node가 이 환경에 없어서, `uv run --with esprima python3`로 세 파일의 `<script>` 내용을 뽑아 esprima(ES5 파서)로 문법 검사만 수행 — 실제 브라우저 동작 테스트는 아니므로, 실사용 중 이상이 있으면 알려달라고 안내함.

## 알려진 제한사항

- 6자리 정밀도로도 초대형 배치(총량이 수백만 g 단위)에서는 극히 드물게 1g 미만의 오차가 여전히 이론상 가능하지만, 실사용 규모(수십 kg 이하)에서는 사실상 발생하지 않는다.
