# 2026-08-18 BOM 스펙 2차 개정 + UI 프로토타입 작업기록

## 배경

`2026-08-18_BOM원가계산_스펙.md` 초판에 대한 피드백 8건을 반영해 2판을 냈고,
이어서 두 가지 추가 요청을 받았다.

1. "계란 무게를 사용 기준으로 하는 걸 원칙으로 하되, 계란 사용 무게를 사용자가 입력해야 할 것 같음."
2. "BOM 본체 작업하고 난 UI를 확인 후 작업을 들어가도 될까?"

## 1. 계란 무게 — 주·보조를 뒤집었다

2판 초안에서는 `unit_weight_g`(구매 기준 무게)가 주 입력이고 `usable_weight_g`(사용 무게)가
보조(null이면 같은 값)였다. **이걸 뒤집었다.**

근거: 개수로 구매하는 일반적인 경우, 구매 기준 무게는 계산에 아예 등장하지 않는다.

```
계란 30개 9,000원  →  개당 300원        ← 나눗셈만으로 나옴
                   →  6원/g = 300 ÷ 50  ← 사용 무게만 필요 (60g은 안 쓰임)
```

구매 기준 무게가 필요한 건 "무게로 사면서 개수로 배합하는" 경우뿐이라, 보조로 내리고
UI에서도 "+ 구매 기준 무게 입력 (선택)"으로 접어뒀다.

**알려진 빈틈으로 기록**: 통마늘처럼 무게로 사면서 손질 로스가 있는데 "1개"가 애매한 재료는
두 칸에 비율만 맞춰 넣는 편법이 필요하다. 수율(%) 칸을 따로 두는 게 정공법이지만 이번 범위 밖.

## 2. UI 프로토타입 — 스키마보다 먼저

"UI를 확인 후 작업"이 맞는 순서라고 판단했다. 원래 계획(§9)은 1단계 스키마 → 2단계 엔진 →
3단계 UI였는데, **UI가 틀린 걸 3단계에서 발견하면 이미 데이터를 옮긴 뒤**라 되돌리기 어렵다.

`bom-prototype.html`을 새로 만들었다.

- **DB 연결 없음.** MOCK 데이터로만 동작하고 `file://`로 바로 열린다.
- **디자인 토큰/컴포넌트를 `account.html`에서 그대로 가져왔다** — 나중에 접어 넣기 쉽게,
  그리고 실제 앱에서 어떻게 보일지 정확히 판단할 수 있게.
  (`:root` 토큰, `.acc-row-*` 아코디언, `.tag-chip`→`.chip`, `.io-btn`, `.bottom-nav` 등)
- **숫자가 하드코딩이 아니다.** 스펙 §5의 `rollupCost()`를 실제로 구현해서, 입력을 바꾸면
  원가가 즉시 다시 계산된다 — UI와 계산 알고리즘을 동시에 검증할 수 있다.

담은 화면:
- 품목 목록 + 필터 칩 4종(전체/구매 재료/반죽·반제품/판매 제품), 기본값은 "구매 재료"(§10-2 완화책)
- 구매 재료 상세 — 계란(개수 구매 + 사용 무게), 버터(가격 변화 표)
- 제조 품목 상세 — 묵은반죽(직접 입력) / 소금빵 반죽(배합에서 계산) 토글
- 판매 제품 상세 — 소금빵(원가/판매가/마진 + 구성 라인별 기여액)
- 자동 생성된 미가격 품목 안내(몰트)

### 검산

파이썬으로 교차 검증해 스펙 §5.1의 손계산과 정확히 일치함을 확인했다.

```
소금빵 반죽 = 2,670원 / 1,930g = 1.3834원/g
소금빵 1개  = 83.01 + 216 + 2 = 301.01원 → 301원
판매가 3,000원 → 마진 2,699원 (90%)
```

### 프로토타입에서 실제로 잡힌 설계 결함 하나

**"단가 0"의 두 가지 의미**(스펙 §10-6에서 위험으로만 적어뒀던 것)가 만들자마자 재현됐다.
미가격 판정을 "가격 > 0"으로 했더니 **물(0원)이 "단가 없음"으로 잡혀** 소금빵 전체에 경고가 떴다.

→ 판정 기준을 **가격이 아니라 "입력 여부"**로 바꿨다:
- 구매 품목은 `purchase_amount > 0`인지로 판단(물은 "1kg 0원"이라 구매량이 있으므로 정상)
- 제조 품목은 `manual_cost_per_gram`을 nullable로 두고 `null`과 `0`을 구분
- `'ea'`는 여기에 더해 사용 무게가 없으면 g/pct 라인에서는 여전히 미해결

스펙 §10-6에 이 판정 규칙을 반영했다. **UI를 먼저 만든 것의 첫 번째 소득** —
스키마부터 만들었으면 이 규칙을 잘못 박아둔 채 데이터를 옮겼을 것이다.

## 확인 요청 사항

프로토타입을 열어서 봐주셨으면 하는 것(파일 상단 주석에도 적어둠):

1. 목록에 구매 재료 / 반죽 / 판매 제품이 섞여 있는 게 괜찮은지 — 필터 칩으로 충분한지
2. 펼쳐서 편집하는 아코디언 방식이 맞는지, 아니면 별도 화면으로 들어가야 하는지
3. 소금빵(판매 제품) 화면에서 원가 구성이 한눈에 들어오는지
4. 계란(개수 구매) 입력칸이 이해되는지
5. 묵은반죽의 "직접 입력 / 배합에서 계산" 전환이 자연스러운지

## 3. UI 승인 → 1단계(스키마 + 마이그레이션) 실행 완료

프로토타입을 확인받고("좋은거 같아") 스펙 §9의 1단계로 들어갔다.
`supabase/2026-08-18_bom_migration.sql`을 작성해 **라이브 Supabase 프로젝트에 실제로 적용했다.**

### 실행 중 걸린 것 — 첫 줄 `--`가 복사에서 빠짐

처음 실행에서 `ERROR: 42601: operator too long at or near "===..."`가 났다.
파일 1번 줄이 `-- ====...`(주석)인데 복사 과정에서 앞의 `-- ` 세 글자가 빠져,
Postgres가 `====`를 연산자로 읽은 것. 파일 자체는 정상이었고 다시 붙여넣어 해결.

부수적으로, SQL 에디터가 **마지막 쿼리 결과만** 표시한다는 걸 감안해 검증 쿼리를 재배치했다 —
제일 중요한 개수 확인표를 PART C 맨 뒤로 옮기고, 목록 조회 두 개를 하나로 합쳤다.
(파일 전체를 붙여넣고 Run 하면 확인표가 바로 화면에 나오게)

### 결과 — 개수 검증 전부 통과

| 항목 | 이관 전 → 후 | |
|---|---|---|
| 재료 (ingredients) | 293 → 293 | OK |
| 레시피 (recipes) | 43 → 43 | OK |
| 제품 (products) | 6 → 6 | OK |
| 조합 제품 | 3 → 3 | OK |
| 배합 라인 | 288 → 288 | OK |
| 자동 생성된 미가격 품목 | — → **109** | 참고 |

기존 테이블은 건드리지 않았고, 앱은 여전히 구 테이블을 읽으므로 화면 변화는 없다.

## 4. ★ 실측 데이터가 설계를 두 군데 뒤집었다

109개가 뭔지 확인하는 쿼리를 돌려보니, 단순한 "중복 이름"이 아니었다.

### (a) 사전발효반죽이 6개+ — "묵은반죽은 수동 단가" 결정을 뒤집음

레시피와 이름이 같은 품목 목록에 나온 것:

```
깜빠뉴 탕종 · 단과자 탕종 · 납품 바게트 폴리시 · 르방뒤르 · 르방리퀴드
```

전부 **탕종/폴리시/르방** — 레시피를 따로 만들어두고 다른 레시피의 재료로 쓰는 사전발효반죽이다.
"묵은반죽 하나" 정도로 보고 "수동 단가로 충분하다"고 정했던 전제가 **실제로는 6개 이상,
그것도 주력 반죽**이었다. 수동으로 두면 밀가루값이 바뀔 때마다 6번을 손으로 다시 계산해야 하고,
그게 이 리뉴얼을 시작하게 만든 바로 그 불편이다.

→ 확인 질문으로 **"일회성 연결 제안 화면"** 확정(스펙 §3.3b).
재료 행마다 링크가 뜨는 UI(2026-07-28에 두 번 걷어낸 것)가 아니라,
정리할 때 딱 한 번 목록으로 보여주고 체크한 것만 연결하는 방식이다.

### (b) 자동 병합은 위험하다 — 실제 오탐이 있었다

"비슷한 이름" 목록에 **다른 재료인데 이름만 겹치는 쌍**이 섞여 있었다:

| 진짜 중복 | 자동 병합하면 안 되는 것 |
|---|---|
| S-500 계량제 / S-500 계량제 외국산 | **소금** / **꽃소금** |
| T65 / 베이크플러스 T65 | **탕종** / **깜빠뉴 탕종** |
| 꿀 / 소백산 벌꿀 아카시아 꿀 | |

"한쪽 이름이 다른 쪽에 통째로 들어감" 규칙으로는 이 둘을 구분할 수 없다.
→ 정리 화면은 **제안만 하고 결정은 사람이** 한다(기본 체크 해제).
CSV 가져오기의 "확인 후 적용" 미리보기와 같은 원칙.

스펙에 §8.3(정리 화면)을 신설하고, §9 실행 계획에 3.5단계로 넣었다.

## 5. 2·3단계 — 롤업 엔진 + 품목 화면 구현 (같은 날 이어서)

"바로 진행하자, 가격이력도 진행하자"는 요청으로 2단계와 3단계를 함께 넣었다.

### 왜 2단계만 따로 하지 않았나

엔진만 넣으면 아무 데서도 안 쓰는 죽은 코드가 되고, 더 중요하게는 **데이터가 갈라진다**.
마이그레이션으로 만들어진 `items`는 그 시점의 복사본이라, 구 재료 관리에서 재료를 고치면
`ingredients`만 바뀌고 `items`는 안 바뀐다. 그래서 **편집 지점을 새 화면 한 곳으로 모으는 것**까지
같이 해야 안전하다고 판단했다(스펙 §10-7이 예상한 병행 기간 위험).

### 구현 — 기존 코드는 한 줄도 수정하지 않았다

`account.html`에 **추가만** 했다(+약 700줄). 기존 함수·화면은 그대로 두고:

- CSS: `.item-chip` / `.kind-badge` / `.cost-summary` / `.bom-line` / `.seg` / `.price-history`
  (나머지는 `.acc-row-*`, `.field-row`, `.io-btn` 등 기존 컴포넌트 재사용)
- HTML: `#itemsPage` 서브페이지 하나
- JS: `rollupCost()` / `sellableUnitCost()` / `purchasedUnitCost()` / `isItemPriced()` +
  목록·상세 렌더러 3종(구매 재료 / 제조 품목 / 판매 제품) + 가격 이력 로드·표시
- 배선: 하단 "재료" 탭 → `openItemsModal()` (**이 한 줄만 되돌리면 원래대로 복구**)

구 재료 관리는 CSV 가져오기가 아직 거기 있어서 계정 메뉴에
`#openLegacyIngredientsBtn`("재료 CSV 가져오기 (구버전)")으로 남겨뒀다.

### 가격 이력 (스펙 §7) — 절반 구현

- **표시**: 구매 재료를 펼치면 `item_price_history`를 그 품목 것만 불러와
  날짜 / 가격 / 원g당 / 증감률(▲▼%)을 표로 보여준다. 마이그레이션이 이관 시점 단가를
  이미 한 줄씩 넣어놨으므로 기준선이 있다.
- **쌓기(수동 편집)**: 구매량/단위/가격을 고치면 `appendPriceHistory()`가 이력을 **덮어쓰지 않고
  한 줄 추가**한다. 값이 실제로 달라졌을 때만 쌓아서, 같은 값으로 저장을 반복해도 이력이 안 늘어난다.
  저장은 `input`이 아니라 `change`(확정) 시점 — 안 그러면 타이핑 중간값이 이력에 남는다.
- **아직 안 한 것**: CSV 가져오기가 이력을 쌓는 것(§7.3, 날짜 칸 매핑 포함). 4단계로 남겼다.

### 검증

- esprima 문법 검사 통과, 중복 `id` 없음, 새 요소 8개 모두 정확히 1번씩 존재.
- **이식한 엔진을 파이썬으로 그대로 재현해 검산** — 프로토타입/스펙 §5.1의 손계산과 완전히 일치:
  소금빵 반죽 1.3834원/g(산출 1930g), 소금빵 1개 301원, 마진 2,699원(90%),
  계란 개당 300원·6원/g, 물은 0원이지만 "미입력" 아님, 몰트(구매량 0)는 "미입력".
- 실제 브라우저 동작 테스트는 아직 — 라이브에서 확인 필요.

## 6. 재료 선택 드롭다운에 단가·갱신일 표시 (같은 날, 별건)

새 화면과는 별개로, **지금 쓰고 있는 원가 계산 화면**에서 요청이 들어왔다 — 재료를 연결할 때
후보가 이름만 나와서 고를 수가 없다는 것. 스크린샷의 실제 예:

```
데코화이트(녹지않는설탕)-선인
설탕(대한제당)
설탕(대한제당) 원당 100%
흑설탕 (대한제당)*
```

이름만으로는 어느 게 지금 쓰는 단가인지, 언제 갱신된 값인지 알 수 없다.

### 선행 수정 — `ingredients.updated_at`이 죽은 데이터였다

갱신일을 보여주려다 발견: `saveFields()`도 CSV 가져오기의 update도 `updated_at`을
**한 번도 채우지 않았고** DB 트리거도 없어서, 생성 시각에 영원히 머물러 있었다.
`recipes.updated_at`이 2026-07-25에 정확히 같은 이유로 죽어 있던 것과 판박이다.
두 저장 경로 모두 payload에 `updated_at`을 명시적으로 넣도록 고쳤다.

> **이 앱에는 `updated_at` 자동 갱신 트리거가 어느 테이블에도 없다.**
> 새 update 경로를 만들 때마다 직접 넣어야 한다 — 이번이 같은 함정에 두 번째로 걸린 것이다.

### 구현

`ingredientPickerMeta(ig)`가 후보마다 두 번째 줄을 만든다:
**단가(원/g) · 구매 정보(N원/Mkg) · 갱신일 · 사용중지 여부**

`'ea'`(개수) 단위는 `price_per_gram`이 0인 게 정상이라 "단가 없음"이 아니라
**"개수 단위 (원가 미반영)"** 로 따로 표시한다 — 안 그러면 멀쩡한 재료가 문제 있는 것처럼 보인다.

조합 제품의 구성요소 선택 드롭다운(`attachComponentPicker()`)에도 같이 적용했다.
거기서 레시피 후보는 구매 정보가 없으므로 `costPerGramForRecipeId()`로 계산한 g당 원가만 보여준다.

## 7. 실사용 버그 두 개 — 가격 저장 실패 / 삭제 불가 (2026-08-20)

새 재료 화면을 실제로 써보니 바로 두 가지가 막혔다. 둘 다 실사용 스크린샷으로 발견.

### (1) 가격을 입력하면 저장 실패

> 저장 실패: Could not find the 'price_per_gram' column of 'items' in the schema cache

**원인**: `items` 테이블에는 `price_per_gram` 컬럼이 **없다**. 단가는 저장하는 값이 아니라
`purchasedUnitCost()`가 구매량/단위/가격에서 그때그때 계산하는 파생값이고, "그 시점의 단가"를
보존하는 건 `item_price_history.price_per_gram` 쪽 하나뿐이다(스펙 §7 — 덮어쓰지 않고 append).
그런데 3단계 구현에서 `items` update payload에 `price_per_gram`을 같이 넣는 코드가
두 군데 남아 있었다(구매 정보 저장, 개수 단위의 사용 무게 저장). PostgREST는 없는 컬럼이
payload에 있으면 **전체 요청을 거부**하므로, 가격 저장 자체가 통째로 실패했다.

`ingredients`(구 테이블)에는 `price_per_gram` 컬럼이 실제로 있어서, 구 화면 코드를
참고하며 옮기는 과정에서 그대로 따라 들어온 것으로 보인다. **구 테이블과 `items`는
단가를 다루는 방식이 다르다** — 구 쪽은 저장, 새 쪽은 파생. 앞으로 구 코드에서
가져올 때 이 차이를 먼저 확인할 것.

### (2) 추가한 재료를 지울 수 없다

상세에 "사용중지"(소프트 삭제)만 있고 완전 삭제 버튼이 아예 없었다.
잘못 추가한 재료가 목록에 영구히 남는다.

**세 종류(구매/제조/판매) 상세 전부에 공통으로 붙는 `appendItemDeleteBtn()`을 추가**했다
(`renderItemsList()`의 상세 분기 직후 호출 — 종류별 빌더 세 곳에 각각 넣지 않는다).

**구 재료 관리와 달리 "사용중지 먼저" 게이트를 두지 않았다.** 구 화면에서 그 2단계가
필요했던 이유는 레시피가 재료를 jsonb 안의 id로만 참조해서 DB가 막아주지 못했기 때문인데,
`bom_lines.child_item_id`는 `on delete restrict`라 **DB가 직접 거부한다** — 훨씬 강한 보장이
이미 있으므로 UI 게이트는 중복이다. 대신 원시 FK 에러 문구를 그대로 보여주지 않으려고,
`itemsUsing(itemId)`가 `bomLinesByParent`를 훑어 **어느 배합에서 쓰고 있는지 이름으로 먼저
알려주고 막는다**(스펙 §10-4). `openItemsModal()`이 열릴 때마다 `loadBomLines()`를 부르므로
이 검사는 항상 최신이다.

### 선행 수정 — `items.updated_at`도 같은 함정

`saveItemFields()`도 `updated_at`을 안 채우고 있었다. 6절에서 `ingredients.updated_at`이,
2026-07-25에는 `recipes.updated_at`이 같은 이유로 죽어 있었다 — **세 번째다.**
이번엔 개별 호출부가 아니라 `saveItemFields()` 안에서 payload에 항상 얹도록 해서,
앞으로 이 함수를 쓰는 새 저장 경로는 자동으로 따라오게 했다.

### 작업 시 주의 — 셸 heredoc이 한글을 깨뜨린다

이 수정 중에 `python3 - <<'PY'` 형태로 패치 스크립트를 넣었더니 한글 주석/문구가
전부 CP949로 변환돼 깨진 채 파일에 들어갔고, `\n` 이스케이프도 한 겹 풀려 실제 개행이
되면서 JS 문법 오류가 났다. **한글이 들어가는 패치는 스크립트 파일로 먼저 쓴 뒤 실행할 것.**
패치 후에는 (a) `esprima`로 `<script>` 블록 파싱, (b) 수정 구간을 다시 읽어 한글 확인,
두 가지를 모두 하는 게 안전하다 — 깨진 한글은 조용해서 파싱만으로는 안 걸린다.

## 8. 4단계 — 재료의 유일한 출처를 `items`로 (2026-08-20)

### 발단

> "원가 계산에서 토판염 입력하고 새 재료 추가 눌러서 추가했음. 재료탭에서 토판염이 안 보임."

원가 계산 화면은 아직 구 화면이라 `ingredients`에 쓰고, 새 재료 탭은 `items`를 읽는다.
그래서 안 보였다. 그런데 확인해보니 **훨씬 심각한 게 같이 있었다** — 새 재료 탭에서 입력한
단가가 원가 계산에 **전혀 반영되지 않고 있었다.** 원가 계산은 여전히
`ingredients.price_per_gram`을 읽고 있었기 때문. 3단계 배포 때 경고했던 "병행 기간 데이터 분기"가
그대로 터진 것이고, 사용자는 이미 새 화면에서 가격을 넣고 있었다.

임시 다리를 놓는 안과 4단계를 앞당기는 안을 제시했고, **4단계 진행**으로 확정했다.

### 무엇을 바꿨나

**`loadMyIngredients()`가 `ingredients` 대신 `items`(kind='purchased')를 읽는다.**
이 한 곳만 바꾸면 그 배열을 쓰는 나머지가 전부 따라온다 — 재료 연결 드롭다운,
`computeCostBreakdown()`의 단가 조회, 조합 제품 구성요소 선택, CSV 내보내기,
CSV 가져오기의 기존값 비교까지.

**기존 연결이 안 끊기는 이유 — `legacy_ingredient_id`를 id로 노출한다.**
레시피가 저장해 둔 `ingredientId`는 구 `ingredients.id`다. `itemToIngredientView()`가
`id: it.legacy_ingredient_id || it.id`로 내보내므로 예전 연결이 그대로 풀린다.
마이그레이션 이후 새로 만든 품목은 legacy가 없으니 자기 `items.id`를 쓴다 — 둘 다 유일해서
섞여도 안전하다.

**덤으로 고쳐진 것**: 구 `computePricePerGram()`은 `'ea'`(개수)면 무조건 0을 돌려줬다.
새 `purchasedUnitCost()`는 `usable_weight_g`가 있으면 실제 g당 단가를 계산한다 — 즉
**사용 무게를 넣은 개수 단위 재료가 이제 원가에 제대로 반영된다**(스펙 §12).

### CSV 가져오기/내보내기를 새 재료 화면으로

마크업(버튼·파일 입력·컬럼 매핑·미리보기)을 통째로 `#itemsPage`로 옮겼다. 적용 단계는
`items`에 insert/update하고 **`item_price_history`에도 같이 남긴다** — 거래명세서를 가져올
때마다 가격 변화가 쌓이는 것이 CSV를 `items`로 옮긴 가장 큰 이유다(스펙 §7.3).

미리보기가 떠 있는 동안 목록을 가리는 `setItemsScreenChrome(visible)`도 새 화면 기준으로
다시 썼다(예전엔 구 화면의 목록/추가 버튼을 가렸다).

**구 "재료 관리" 화면은 이제 어디서도 열리지 않는다** — 계정 메뉴의
"재료 CSV 가져오기 (구버전)" 항목을 제거했다. 마크업과 JS는 그대로 남겨뒀는데,
지금 마크업만 지우면 아직 남아있는 `getElementById`가 전부 null이 되어 초기화가 깨진다.
6단계에서 구 테이블과 함께 한 번에 지운다.

### 원가 계산의 "+ 새 재료로 추가"에서 단가 바로 입력

> "새 재료 추가 할 때 구매단가를 바로 입력하게 할 수 있음?"

`openInlinePriceEntry()` — 재료를 만든 직후 그 자리에 구매량/단위/가격 패널이 뜨고,
입력하는 동안 "→ 1g당 N원"을 미리 보여준다. 저장하면 `items`와 `item_price_history`에
동시에 쓴다(재료 화면에서 입력한 것과 완전히 같은 경로). "나중에"로 건너뛸 수 있다.

두 가지 주의점이 있어서 코드에 주석으로 남겼다:
- **`onChange()`를 패널이 끝난 뒤에 부른다.** 먼저 부르면 재료 목록이 다시 그려지면서
  패널이 들어있는 DOM이 통째로 날아간다.
- **저장 실패 시 패널을 닫지 않는다.** 닫으면 방금 친 값이 사라진다. 판정은 가격이 아니라
  **구매량**으로 한다 — 가격은 0원이 정상인 재료(물)가 있어서 0을 실패와 구분할 수 없다.

### 수동 마이그레이션 한 줄 필요

`supabase/2026-08-20_step4_drop_legacy_ingredient_fk.sql` — `composite_product_components.ingredient_id`가
구 `ingredients(id)`를 참조하는 외래 키를 갖고 있어서, 마이그레이션 이후 새로 만든 품목을
조합 제품 구성요소로 고르면 저장이 실패한다. 조합 제품 자체가 6단계에서 흡수될 예정이라
제약만 떼고 컬럼은 둔다.

### 남은 것

`items`에 쓰지 않고 구 `ingredients`에만 쓰는 코드가 5곳 남아 있는데 전부 구 재료 관리
화면 안이고, 그 화면은 열리지 않으므로 죽은 코드다. 6단계에서 화면·테이블과 함께 지운다.

## 다음 단계

1. **3.5단계 — 정리 화면**(스펙 §8.3): 자동 생성된 109개 정리.
   사전발효반죽 "연결" 제안 + 표기 흔들림 "병합" 제안, 둘 다 체크한 것만 처리.
2. **5단계 — 저장 시 미러링**: 계산기에서 레시피를 저장할 때 `recipes.flours`/`ingredients`를
   `bom_lines`로 자동 반영(스펙 §6.4).
3. **6단계 — 구 화면/테이블 제거**: 구 재료 관리 마크업+JS, `ingredients`/`products`/
   `composite_products`, `cost_snapshot_*`.

CSV 가져오기의 **날짜 칸 매핑**(거래명세서의 거래일자를 `effective_date`로)은 4단계에서
넣지 않았다 — 지금은 가져온 시점이 이력의 날짜가 된다. 과거 명세서를 소급해서 넣을 일이
생기면 그때 추가한다.

프로토타입 파일(`bom-prototype.html`)은 역할을 다했으므로 6단계에서 지운다.
