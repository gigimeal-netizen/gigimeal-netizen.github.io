# 2026-08-18 조합 제품 원가가 항상 0원으로 나오던 버그 수정 작업기록

## 배경

"원가계산이 너무 불편하다, BOM 개념으로 아예 리뉴얼했으면 좋겠다"는 요청으로
`2026-08-18_BOM원가계산_스펙.md`(설계 스펙, 별도 문서)를 먼저 작성했다.

그 직후 실사용 스크린샷으로 별개의 버그가 보고됐다 — 조합 제품 "쪽파 베이첼"의 구성요소
(프레첼 120g, 쪽파크림치즈 100g)가 **둘 다 "원가 0원 · 단가 없음"**으로 나오는데,
사용자는 "재료에 다 입력했다"는 상황.

## 원인 — 스냅샷 전환에서 호출부 하나가 누락됐다

2026-07-25에 원가 계산을 독립 스냅샷(`recipes.cost_snapshot_flours` /
`cost_snapshot_ingredients`) 기반으로 바꿨다. 그때 레시피 탭 쪽은 전부 스냅샷을 보도록 고쳤는데,
**조합 제품이 쓰는 `costPerGramForRecipeId()`만 예전처럼 라이브 컬럼을 계속 읽고 있었다.**

| 함수 | 읽는 소스 | 상태 |
|---|---|---|
| `renderCostRecipesList()` | `cost_snapshot_*` | 정상 |
| `openCostCalcRecipe()` → `costModalRecipe` | `cost_snapshot_*` | 정상 |
| `renderCostIngredientsList()` | `costModalRecipe` | 정상 |
| `refreshProductCosts()` | `costModalRecipe` | 정상 |
| **`costPerGramForRecipeId()`** (조합 제품) | **라이브 `recipes.flours`/`ingredients`** | **버그** |

재료 DB 연결(`ingredientId`)은 `saveCostRecipeLinks()`가 **`cost_snapshot_*`에만** 쓴다.
메인 계산기(레시피 편집 화면)는 그 필드를 직접 편집하지 않으므로 라이브 컬럼은 대부분
`ingredientId: null` 상태다. 결과적으로 **원가 계산 화면에서 재료를 아무리 연결해도
조합 제품은 연결이 하나도 없는 쪽을 보고 있었다.**

### 두 번째 원인

같은 함수만 `migrateIngredientRow()`(공백/대소문자를 정규화한 이름 자동 매칭)를 **안 돌리고
있었다.** `applyRecipeRowToState()`(메인 편집기)와 `openCostCalcRecipe()`(원가 계산)는 둘 다
돌린다. 그래서 레시피의 재료 이름이 재료 관리의 이름과 정확히 같아도, 조합 제품 경로에서만
연결이 안 잡혔다.

## 수정

`costPerGramForRecipeId()`가 레시피 탭과 같은 소스 선택 규칙을 따르도록 맞췄다.

- `cost_snapshot_taken_at`이 있으면 → 스냅샷 컬럼
- 없으면(원가 계산에서 한 번도 안 연 레시피) → 라이브 컬럼으로 폴백
  (0원을 보여주는 것보다 낫고, `openCostCalcRecipe()`의 "처음 열 때 라이브에서 스냅샷 생성"과 같은 취지)
- 두 경로 모두 `migrateIngredientRow()`를 적용 — 단, `Object.assign({}, row)` **사본**에 적용해서
  `myRecipes` 캐시를 제자리 수정하지 않는다(`openCostCalcRecipe()`와 같은 방식).

## 검증

- `uv run --with esprima python3`로 `<script>` 문법 검사 — PARSE OK.
- `computeRecipeCostPerGramFor()`/`computeCostBreakdown()`의 **모든 호출부 4곳을 전수 확인**해서
  이 함수 외에 라이브/스냅샷이 어긋난 곳이 더 없는지 대조했다 — 나머지 3곳은 전부 정상.
- 실제 브라우저 동작 테스트는 아니므로, 배포 후 조합 제품 원가가 실제로 잡히는지 확인 필요.

## 남은 확인 사항 (수정 후에도 0원이면)

1. 그 레시피를 **원가 계산 → 레시피 탭에서 한 번 열어본 적이 있는지** — 스냅샷이 없으면
   라이브 폴백 + 이름 자동 매칭에 의존한다.
2. 레시피의 재료 **이름이 재료 관리의 이름과 일치**하는지(공백/대소문자는 무시됨).
3. 재료 관리에서 그 재료의 **구매량·구매가가 실제로 입력**돼 `price_per_gram`이 0보다 큰지.
4. 구매 단위가 **`'ea'`(개수)면 `price_per_gram`이 항상 0**이라 원가에 안 잡힌다 —
   이건 현재 구조의 알려진 한계이고, BOM 스펙(§2.6, §4.1의 `unit_weight_g`)에서 다룬다.

## 관련

이 버그는 BOM 스펙에서 지적한 구조적 문제(§2.3 "원가 계산 로직이 세 벌로 흩어져 있다",
§2.4 "스냅샷이 복잡도의 큰 원인")의 구체적 사례다. 계산 경로가 하나였다면 애초에 생길 수 없었다.
