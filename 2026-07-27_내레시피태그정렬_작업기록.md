# 2026-07-27 "내 레시피" 태그 필터 + 순서 편집 작업기록

## 배경

스크린샷으로 `account.html`의 라이브 사이트(`gigimeal-netizen.github.io/mino-account`) 첫 화면을 확인해보니, "공유받은 레시피"가 별도 카드로 따로 떠 있어 화면이 난잡했다. 조사 결과:

- 로컬 `account.html`에는 이미 그 섹션(`#sharedWithMeSection`)을 없애고, 소유 여부와 무관하게 `myRecipes` 전체를 "내 레시피" 목록(`#myRecipesListPage`)에 합친 상태였다 (코드 주석에 2026-07-27 날짜로 이미 기록돼 있었음 — 이 세션 이전의 작업).
- 문제는 그 수정이 **`mino` 저장소에도 커밋되지 않았고**(`account.html`이 마지막 커밋 대비 2985줄 추가/292줄 삭제 상태로 미커밋), **`mino-account`(실제 배포) 저장소에는 더더욱 반영이 안 돼 있었다**(마지막 배포 커밋이 그보다도 훨씬 이전 버전) — 그래서 라이브 사이트만 구식 UI를 계속 보여주고 있었던 것.

이 발견을 계기로, 사용자가 이어서 요청한 "태그별로 분류", "레시피/태그 위치 이동" 기능을 함께 구현했다.

## 결정 사항 (확인 질문으로 결정)

1. **태그 활용 방식**: 필터 칩 추가 (그룹 보기로 완전 전환하지 않음 — 기존 "태그별 보기" 화면은 그대로 유지, 용도가 다름: 그룹 보기+공유 vs 내 목록 좁혀보기)
2. **순서 이동 UI**: 위/아래 화살표 버튼 (드래그 아님 — 터치 환경 안정성, `index.html`/`manager.html`의 재료 순서 이동과 같은 방식)
3. **순서 저장**: 서버(Supabase)에 저장해서 기기 간 유지
4. **커밋/배포 시점**: 새 기능까지 다 만든 뒤 한 번에

## 구현

### 태그 필터 칩 (`#myRecipesListPage`)
- `renderMyRecipesTagBar()`가 `myRecipes`(소유+공유받은 것 전부)에서 태그를 평탄화·중복제거해 칩으로 그린다. "전체" 칩이 항상 맨 앞, 탭하면 단일 선택 필터로 목록이 좁혀짐(다시 탭하면 해제). 기존 이름/태그 검색어 필터와 AND로 함께 적용.

### 순서 저장 — 뷰어 기준 별도 테이블
- 새 `recipe_list_order`/`tag_list_order` 테이블(`supabase/schema.sql`) — `user_id` + `recipe_id`/`tag` + `sort_order`, owner-only RLS.
- `recipes` 행 자체에 순서 컬럼을 두지 않은 이유: 공유받은 레시피는 RLS상 소유자만 그 행을 update할 수 있어서, 공유받은 사람이 자기 화면에서만 순서를 바꾸려 해도 원천적으로 안 된다. 뷰어 기준 별도 테이블이라야 소유자든 공유받은 사람이든 각자 독립적인 순서를 가질 수 있다.
- 명시적 순서가 없는 항목은 정렬 시 맨 뒤로 밀리되, `Array#sort`가 안정 정렬이라 그런 항목들끼리는 기존 폴백 순서(레시피는 `updated_at` 내림차순, 태그는 첫 등장 순)가 유지된다.

### 순서 편집 UI
- 기존 `.reorder-btns`(▲▼ 텍스트 글리프) 패턴을 그대로 재사용. "순서 편집" 토글을 켜면 태그 칩 바가 세로 목록으로, 레시피 행도 클릭-로드 대신 이름+▲▼만 있는 행으로 바뀐다.
- `moveRecipeOrder()`/`moveTagOrder()`: 대상 목록에 아직 명시적 순서가 없으면(첫 편집) 지금 화면 순서 그대로 baseline을 매겨 전체를 한 번에 저장하고, 이후로는 바뀐 두 행만 저장 — fractional-index 없이 간단하게 구현.

## 커밋/배포

- `mino` 저장소: `account.html`, `supabase/schema.sql`, 이 작업기록을 커밋.
- `mino-account`(배포) 저장소: `account.html` → `index.html`로 복사 후 커밋/푸시 — 이번 기능뿐 아니라 그동안 밀려있던 풀리시(사전발효반죽) 기능 등도 함께 배포됨.
- Supabase 프로젝트에 `recipe_list_order`/`tag_list_order` 테이블을 만들려면 `supabase/schema.sql`의 새로 추가된 부분을 SQL 에디터에서 실행해야 한다 (기존 프로젝트는 마이그레이션 필요).

## 알려진 제한사항 / 다음 단계

- "태그별 보기"(`#recipeTagsPage`, 그룹핑+공유 전용 화면)는 이번 태그 순서(`tagOrderMap`)를 반영하지 않는다 — 별개 화면으로 남겨둠(요청 범위 밖).
- 순서가 없는 태그/레시피가 많다가 한꺼번에 처음 편집되면 그 시점 화면 순서 전체가 그대로 baseline으로 저장된다 — 의도된 동작.
