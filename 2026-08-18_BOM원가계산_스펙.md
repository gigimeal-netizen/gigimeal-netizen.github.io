# 2026-08-18 원가 계산 / 재료 관리 BOM 전면 리뉴얼 스펙

> 이 문서는 **설계 스펙**(`_스펙`)이지 작업기록이 아니다. 아직 아무것도 구현되지 않았다.
> 기존 `2026-07-14_재료비생산관리_스펙.md` / `2026-07-14_재료비생산관리_스펙수정.md`가 그렸던
> 원가 레이어를 **BOM(Bill of Materials, 자재명세서) 구조로 전면 교체**하는 것을 목표로 한다.

---

## 1. 배경 — 왜 지금 구조로는 안 되는가

사용자 원문:

> 원가계산이 너무 불편함. BOM 개념으로 접근하면 좋겠음.
> 예) 소금빵의 경우 소금빵반죽 60g + 충전용 소금버터 12g + 토핑용 소금 1g으로 만들어짐.
> 그리고 소금빵반죽에는 묵은반죽이 들어감. 이런 이유 때문에 현재 .html로 작업하기에 곤란한 부분이 많이 생김.
> 나는 기존의 원가계산, 재료관리를 아예 리뉴얼했으면 좋겠음.

이 소금빵 하나가 현재 구조의 한계를 정확히 찌른다.

```
소금빵 (판매 제품, 1개 = 73g)
├─ 소금빵 반죽        60g   ← 레시피(배합)
│   ├─ 밀가루         100%
│   ├─ 물              65%
│   ├─ 소금             2%
│   ├─ 이스트           1%
│   ├─ 버터             5%
│   └─ 묵은반죽        20%   ← ★ 레시피 안에 또 다른 레시피
│        ├─ 밀가루    100%
│        ├─ 물         70%
│        └─ 소금        2%
├─ 충전용 소금버터    12g   ← 구매 재료 (또는 그 자체가 배합)
└─ 토핑용 소금         1g   ← 구매 재료
```

**현재 구조로 어디까지 되는가:**

- 소금빵 = 조합 제품(`composite_products`)으로 만들고, 구성요소 3개(반죽 60g / 소금버터 12g / 소금 1g)
  → **여기까지는 된다.**
- 소금빵 반죽 안의 **묵은반죽 20%** → **표현할 방법이 없다.**
  레시피의 재료 행은 `ingredientId`(구매 재료 마스터)만 연결할 수 있고, **다른 레시피는 연결할 수 없다.**

이게 2026-07-28에 두 번 시도했다가 두 번 다 걷어낸 바로 그 지점이다
(재료 행마다 "다른 레시피 연결" 링크 → "더 헷갈린다"로 롤백,
카드당 1회성 "가져오기" 버튼 → 같은 날 다시 제거).

두 번 다 실패한 이유는 UI 배치 문제가 아니라 **데이터 모델이 중첩을 전제하지 않기 때문**이다.
중첩이 예외 케이스인 모델 위에 중첩 UI를 얹으니 어디에 놓아도 이물감이 생겼다.
BOM은 **중첩이 기본값**인 모델이다. 그래서 모델부터 바꿔야 한다.

---

## 2. 현재 구조 상세 분석

### 2.1 개념이 4개 테이블로 쪼개져 있다

| 테이블 | 역할 | 정의 위치 |
|---|---|---|
| `ingredients` | 구매 재료 마스터. 구매량/단위/가격 → `price_per_gram` | `supabase/schema.sql:67` |
| `recipes` | 배합(비율). `flours`/`ingredients` jsonb 배열 | `supabase/schema.sql:11` |
| `products` | 레시피 **1개**에 종속된 판매 단위 | `supabase/schema.sql:86` |
| `composite_products` + `composite_product_components` | 여러 레시피/재료를 조합한 판매 단위 | `supabase/schema.sql:103`, `:118` |

계층이 **정확히 2단계로 고정**되어 있다:

```
composite_product ─┬→ recipe    ─→ ingredient
                   └→ ingredient
product ───────────→ recipe     ─→ ingredient
```

- 레시피는 재료만 참조 가능 → **레시피 → 레시피 불가** (묵은반죽 문제)
- 조합 제품은 다른 조합 제품을 참조 불가 → **3단계 이상 불가**
- 조합 제품은 레시피의 재료가 될 수 없음

### 2.2 "제품"이 두 개의 별개 개념이다

같은 "제품"인데 등록하는 곳이 다르다.

- 레시피 1개 기반 → 원가 계산 탭 안의 "제품 & 원가" (`renderProductsList()`, `account.html:3771`)
- 여러 개 기반 → 별도 "조합 제품" 탭 (`renderCompositeProductsList()`, `account.html:4145`)

사용자는 소금빵을 만들려다 **"이건 제품인가 조합 제품인가"부터 판단**해야 한다.
심지어 나중에 구성이 바뀌면(반죽 하나였는데 토핑이 추가되면) 한쪽에서 지우고 다른 쪽에서 다시 만들어야 한다.

### 2.3 원가 계산 로직이 세 벌로 흩어져 있다

같은 "원가"를 세 함수가 각자 계산한다.

| 함수 | 위치 | 하는 일 |
|---|---|---|
| `computeCostBreakdown()` | `account.html:2908` | 레시피의 비율로 g당 원가 + 재료별 기여도 |
| `refreshProductCosts()` | `account.html:3739` | 제품 행별 원가/마진, DOM 입력값 직접 재조회 |
| `refreshCompositeCost()` | `account.html:4273` | 조합 제품 구성요소별 원가 합산 |

세 곳 모두 `effectiveCostPerGram()`(`account.html:3734`)을 각자 호출하고,
각자 "단가 없음" 경고를 각자 방식으로 표시한다. 새 계층이 하나 생기면 **네 번째 사본**이 필요해진다.

### 2.4 원가 스냅샷(`cost_snapshot_*`)이 복잡도의 큰 원인

2026-07-25에 "원가 계산이 레시피 편집을 실시간으로 따라가면 안 된다"는 요청으로 도입됐다.

- `recipes.cost_snapshot_flours` / `cost_snapshot_ingredients` / `cost_snapshot_taken_at`
- 처음 열 때 자동 스냅샷 (`openCostCalcRecipe()`, `account.html:6267`)
- 원본이 바뀌면 "⚠ 원본 레시피가 이후에 수정됐어요" 배너 → 사용자가 **수동으로** "다시 불러오기"
  (`reloadCostSnapshot()`, `account.html:6299`)
- 다시 불러오면 이름 기준으로 재료 연결을 자동 승계 (`mergeSnapshotIngredientIds()`)

문제: **중첩이 생기면 이 방식은 관리 불가능해진다.**
묵은반죽을 고쳤을 때 소금빵반죽 스냅샷도, 소금빵 원가도 모두 오래된 것이 된다.
N단계 중첩이면 N개의 "다시 불러오기"를 사용자가 순서대로 눌러야 한다.

### 2.5 이름과 원가 연결이 이중 입력이다

2026-07-24에 의도적으로 분리한 구조다 — 그 결정 자체는 타당했다:

> 레시피는 "버터"라고 짧게 쓰는데 실제 구매품은 "앵커버터"다.
> 레시피 쓰면서 매번 DB를 매칭하는 건 마찰이고, 게다가 고르면 레시피의 이름이 브랜드명으로 덮어써진다.

그래서 지금은:
1. **계산기**에서 "버터"라고 자유 텍스트로 입력 (`.ing-name`, 연결 안 함)
2. **원가 계산 화면**에서 그 행을 "앵커버터"에 다시 연결 (`saveCostRecipeLinks()`, `account.html:6344`)

이름을 지키려다 **입력을 두 번 하게 됐다.** 레시피가 20개면 연결 작업도 20번이다.
(`migrateIngredientRow()`의 이름 자동 매칭과 Levenshtein 오타 제안이 이 마찰을 줄이려는 보조 장치다 —
즉 이 구조 자체가 마찰이라는 방증이기도 하다.)

### 2.6 그 밖의 구조적 결함

- **`'ea'`(개수) 단위 재료는 원가가 항상 0** — `computePricePerGram()`(`account.html:2886`)이
  `'ea'`면 무조건 `0`을 반환한다. 소금버터를 "1박스 100개 30,000원"으로 사면 원가에서 통째로 빠진다.
  "1개 = 12g" 같은 환산 정보를 넣을 자리가 없다.
- **제조 로스를 표현할 곳이 없다** — 지금의 `loss_rate_pct`는 전부 *성형/재단* 로스
  (`products.loss_rate_pct`, `composite_product_components.loss_rate_pct`)다.
  믹싱 손실, 발효 감량, 굽기 수분 증발 같은 **배합 자체의 수율**은 넣을 데가 없다.
- **산출량(yield) 개념이 암묵적** — `computeCostBreakdown()`이 재료 무게 합을 그대로 산출량으로 쓴다.
  "밀가루 1000g 배합 → 실제로 1650g 나옴"을 표현할 수 없다.
- **순환 참조 대비가 없다** — 2026-07-28에 `costPerGramRecursionGuard`를 급히 넣었다가
  기능 롤백과 함께 같이 제거됐다. 중첩을 정식 지원하려면 반드시 필요하다.
- **파일 크기** — `account.html`이 7,004줄, 함수 206개. 이 구조 위에 계층을 더 얹으면 유지보수가 어렵다.

---

## 3. BOM 재설계 — 핵심 개념

### 3.1 하나의 개념: "품목(Item)"

지금의 4개 개념(재료 / 레시피 / 제품 / 조합 제품)을 **품목 하나**로 통합한다.

```
품목(Item)
├─ 구매 품목 (purchased) — 사서 쓰는 것. 단가를 직접 입력. BOM 트리의 잎(leaf).
└─ 제조 품목 (made)      — 만들어 쓰는 것. 하위 품목 목록(BOM)을 가짐. 단가는 계산됨.
```

"반죽"과 "완제품"을 구분하지 않는다. **둘 다 제조 품목이다.**
차이는 `is_sellable`(판매가를 갖는가) 플래그 하나뿐이다.

이것이 왜 중요한가: 소금빵반죽은 지금 "레시피"라서 다른 레시피에 못 들어간다.
BOM에서는 소금빵반죽도 묵은반죽도 소금빵도 전부 같은 종류의 품목이므로,
**"A가 B를 포함한다"는 관계 하나로 모든 경우가 표현된다.**

### 3.2 하나의 관계: "BOM 라인"

```
BOM 라인 = (상위 품목, 하위 품목, 수량, 단위, 로스율)
```

소금빵 예시가 전부 같은 형태로 표현된다:

| 상위 | 하위 | 수량 |
|---|---|---|
| 소금빵 | 소금빵 반죽 | 60g |
| 소금빵 | 소금버터 | 12g |
| 소금빵 | 소금 | 1g |
| 소금빵 반죽 | 밀가루 | 100% |
| 소금빵 반죽 | 묵은반죽 | 20% |
| 묵은반죽 | 밀가루 | 100% |

**깊이 제한이 없다.** 묵은반죽 안에 또 뭔가 들어가도 같은 방식이다.

### 3.3 베이커스 퍼센트와 BOM은 충돌하지 않는다

이 앱의 정체성은 베이커스 퍼센트 계산기다. BOM은 절대 수량 기반이라 충돌해 보이지만, 아니다.

**BOM 라인의 단위를 3종으로 두면 둘 다 표현된다:**

- `pct` — 밀가루 100% 기준 비율 (제빵 배합)
- `g` — 절대 중량 (제과, 완제품 조립)
- `ea` — 개수 (계란 3개 등)

같은 BOM 라인 테이블에 `qty_unit`만 다르게 저장한다.
계산기는 `pct` 라인을 읽어 지금처럼 동작하고, 원가 엔진은 셋 다 g으로 환산해 롤업한다.

### 3.4 산출량(yield)을 명시한다

원가 롤업의 핵심 값이다.

```
제조 품목의 단위 원가 = (하위 품목 원가 합계) ÷ (실제 산출량)

실제 산출량 = 배합 산출 무게 × (1 − 제조 로스율)
```

- **배합 산출 무게**: 비워두면 하위 라인 무게 합계로 자동 계산 (지금 동작과 동일 → 마이그레이션 무손실)
- **제조 로스율**(`process_loss_pct`): 믹싱/발효/굽기 감량. 새로 생기는 개념.

### 3.5 로스율 두 종류를 분리한다

지금 뒤섞여 있는 것을 명확히 나눈다.

| 종류 | 저장 위치 | 의미 | 예 |
|---|---|---|---|
| **제조 로스** | `items.process_loss_pct` | 이 품목을 만들 때의 손실 | 믹싱볼 잔량, 발효 감량 |
| **투입 로스** | `bom_lines.loss_pct` | 이 품목을 상위에 넣을 때의 손실 | 성형 파지, 재단 손실 |

현재 `products.loss_rate_pct` / `composite_product_components.loss_rate_pct`는 전부 **투입 로스**이므로
`bom_lines.loss_pct`로 그대로 이관된다. (기존 `effectiveCostPerGram()` 공식 그대로 재사용)

---

## 4. 데이터 모델 (제안)

### 4.1 `items` — 통합 품목 마스터

```sql
create table items (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,

  name text not null,                          -- 레시피에서 부르는 이름 ("버터")
  kind text not null default 'purchased',      -- 'purchased' | 'made'
  category text not null default '',           -- 자유 텍스트 (기존 ingredients.category 계승)
  is_active boolean not null default true,     -- 소프트 삭제 (기존 정책 계승)

  -- kind='purchased' 전용 --------------------------------------------------
  supplier_item_name text not null default '', -- 실제 구매품명 ("앵커버터")
  purchase_amount numeric not null default 0,
  purchase_unit text not null default 'g',     -- 'g'|'kg'|'ml'|'l'|'ea'
  purchase_price numeric not null default 0,
  unit_weight_g numeric,                       -- 'ea'일 때 1개의 무게. 있으면 g 환산 가능
  price_per_gram numeric not null default 0,   -- 계산 결과 캐시 (기존과 동일)
  price_per_ea numeric not null default 0,     -- 'ea' 단위용 (신규 — 개당 원가 계산 가능해짐)

  -- kind='made' 전용 -------------------------------------------------------
  recipe_id uuid references recipes(id) on delete set null,  -- 배합 편집은 계산기에서 (§4.4)
  formula_mode text not null default 'weight', -- 'bakers_pct' | 'weight'
  batch_yield_g numeric,                       -- null이면 하위 라인 무게 합계로 자동
  process_loss_pct numeric not null default 0, -- 제조 로스

  -- 판매 품목일 때 ---------------------------------------------------------
  is_sellable boolean not null default false,
  selling_price numeric not null default 0,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

**`name` vs `supplier_item_name` 분리가 2.5의 이중 입력 문제를 푼다.**
레시피에는 "버터"로 뜨고, 재료 관리에는 "버터 → 앵커버터 10kg 100,000원"으로 관리된다.
브랜드명이 레시피 표시를 덮어쓰지 않으므로, **연결을 별도 화면으로 분리할 이유가 사라진다.**

### 4.2 `bom_lines` — 품목 간 포함 관계

```sql
create table bom_lines (
  id uuid primary key default gen_random_uuid(),
  parent_item_id uuid not null references items(id) on delete cascade,
  child_item_id  uuid references items(id) on delete restrict,  -- 참조 중이면 삭제 차단

  display_name text not null default '',  -- 이 배합에서만 쓰는 표시명 (비우면 child.name)
  qty numeric not null default 0,
  qty_unit text not null default 'g',     -- 'pct' | 'g' | 'ea'
  loss_pct numeric not null default 0,    -- 투입 로스 (성형/재단)
  is_flour boolean not null default false,-- qty_unit='pct'일 때 100% 기준 밀가루인지
  stage text not null default 'final',    -- 'pre'(사전발효) | 'final' — 기존 풀리시 개념 계승
  sort_order integer not null default 0,

  created_at timestamptz not null default now()
);
create index bom_lines_parent_idx on bom_lines(parent_item_id);
create index bom_lines_child_idx  on bom_lines(child_item_id);
```

- `child_item_id`가 `on delete restrict`인 이유: 어딘가에 쓰이는 품목을 지우면 원가가 조용히 틀어진다.
  기존 `ingredients.is_active` 소프트 삭제 정책과 같은 취지를, 이번엔 DB가 강제한다.
- `is_flour`가 기존 `state.flours` / `state.ingredients` 두 배열 분리를 대체한다.
- `display_name`은 "이 배합에서만 다르게 부르고 싶을 때"의 탈출구다 (예: 같은 소금을 "토핑용 소금"으로).

### 4.3 순환 참조 방지

BOM은 반드시 DAG여야 한다. A가 B를 포함하는데 B가 A를 포함하면 원가 계산이 무한 재귀한다.

**DB 트리거로 강제**한다 (클라이언트 검사만으로는 부족 — 여러 탭/기기에서 동시에 편집 가능):

```sql
create or replace function check_bom_cycle()
returns trigger language plpgsql as $$
begin
  if new.child_item_id is null then return new; end if;
  if new.child_item_id = new.parent_item_id then
    raise exception '품목은 자기 자신을 포함할 수 없어요';
  end if;
  -- 재귀 CTE로 child의 하위 트리에 parent가 있는지 확인
  if exists (
    with recursive descendants as (
      select child_item_id as id from bom_lines where parent_item_id = new.child_item_id
      union
      select b.child_item_id from bom_lines b
        join descendants d on b.parent_item_id = d.id
    )
    select 1 from descendants where id = new.parent_item_id
  ) then
    raise exception '순환 참조가 생겨요 — 이 품목은 상위 품목 안에 이미 포함돼 있어요';
  end if;
  return new;
end; $$;

create trigger bom_lines_no_cycle before insert or update on bom_lines
  for each row execute function check_bom_cycle();
```

### 4.4 `recipes`와의 관계 — 무엇을 남기고 무엇을 옮기는가

**이것이 이 리뉴얼의 가장 중요한 결정이다.**

`recipes` 테이블에는 원가와 무관한 기능들이 대거 엮여 있다:
공유(`recipe_shares` / `tag_account_shares` / `share_token` / 초대 링크),
아카이브(`is_public` / `author_display_name` / `recipe_reports`),
태그(`tags` / `tag_shares` / `recipe_list_order` / `tag_list_order`), CSV 내보내기/가져오기.

이걸 전부 `items`로 옮기면 **공유·아카이브·태그 기능을 전부 다시 만들어야 한다** — 범위가 통제 불가능해진다.

**제안: 역할을 나눈다.**

| 남는 곳 | 옮기는 것 |
|---|---|
| `recipes` — 이름, 태그, 공유, 아카이브, 모드/배율/총반죽량/분할, 메모 | 그대로 유지 |
| `bom_lines` — 배합 내용 자체 | `recipes.flours` / `recipes.ingredients` jsonb에서 **이관** |

즉 `recipes` 1행 ↔ `items`(kind='made') 1행이 `items.recipe_id`로 1:1 대응하고,
**배합 내용의 유일한 진실은 `bom_lines`**가 된다.

이 결정의 결과:
- 계산기가 jsonb 대신 `bom_lines`를 읽고 쓴다 → 원가 엔진과 **같은 데이터를 본다**
- `cost_snapshot_*` 3개 컬럼이 **통째로 불필요해진다** (§2.4의 복잡도가 사라짐)
- "원본 수정됨 → 다시 불러오기" 흐름도 사라진다 (하나의 진실이므로 항상 최신)
- 재료 연결 이중 입력도 사라진다 (§2.5)

**대가**(반드시 같이 처리해야 함):
- 공유/아카이브로 레시피를 볼 때 `bom_lines`도 함께 읽어야 함 → RLS 정책 확장 필요
  (`recipes`의 "shared read access" / "public recipes are readable by anyone"과 대칭되는 정책을 `bom_lines`/`items`에도)
- 공유받은 레시피의 하위 품목(묵은반죽)까지 보여줄지 결정 필요 → **§9 열린 질문 Q3**
- CSV 내보내기/가져오기가 `bom_lines` 기준으로 재작성되어야 함
- 레시피 삭제 시 대응 `items` 행 정리 (또는 `items`를 남기고 `recipe_id`만 null)

---

## 5. 원가 롤업 엔진

### 5.1 알고리즘

세 곳에 흩어진 계산(§2.3)을 **함수 하나**로 통일한다.

```js
// 반환: { costPerGram, costPerEa, unpriced: [품목명...], yieldG }
function rollupCost(itemId, memo, visiting) {
  if (visiting.has(itemId)) throw new CycleError(itemId);   // DB 트리거의 2차 방어선
  if (memo[itemId]) return memo[itemId];                    // 같은 품목이 여러 번 쓰여도 1회만 계산

  const item = itemsById[itemId];

  // 잎: 구매 품목 — 단가를 그대로 반환
  if (item.kind === 'purchased') {
    return memo[itemId] = {
      costPerGram: item.price_per_gram,
      costPerEa:   item.price_per_ea,
      unpriced:    (item.price_per_gram > 0 || item.price_per_ea > 0) ? [] : [item.name],
      yieldG:      null
    };
  }

  // 가지: 제조 품목 — 하위를 재귀 롤업해 합산
  visiting.add(itemId);
  const lines = bomLinesByParent[itemId] || [];
  const flourG = totalFlourGrams(lines);   // qty_unit='pct' 라인의 기준 무게

  let totalCost = 0, totalWeight = 0, unpriced = [];
  for (const line of lines) {
    const child = rollupCost(line.child_item_id, memo, visiting);
    const g = toGrams(line, flourG, child);              // pct/g/ea → g 환산
    const lineCost = costOfLine(line, child, g);         // 단위에 맞는 단가 × 수량
    // 투입 로스: L%가 버려지면 1g 쓰려고 1/(1-L) g을 투입해야 함 (기존 effectiveCostPerGram과 동일)
    const loss = clamp(line.loss_pct, 0, 99) / 100;
    totalCost   += loss > 0 ? lineCost / (1 - loss) : lineCost;
    totalWeight += g;
    unpriced.push(...child.unpriced);
  }
  visiting.delete(itemId);

  // 산출량: 명시값 우선, 없으면 라인 무게 합계. 제조 로스 반영.
  const batchG = item.batch_yield_g ?? totalWeight;
  const yieldG = batchG * (1 - clamp(item.process_loss_pct, 0, 99) / 100);

  return memo[itemId] = {
    costPerGram: yieldG > 0 ? totalCost / yieldG : 0,
    costPerEa:   null,
    unpriced, yieldG
  };
}
```

**핵심 성질:**
- 메모이제이션 덕에 같은 품목이 트리에 여러 번 나와도 한 번만 계산 (밀가루가 3단계에 다 나와도 1회)
- `unpriced` 배열이 **어느 품목의 단가가 비었는지 이름으로 알려준다**
  (지금은 "단가 없음" 플래그만 있어서 어디를 고쳐야 할지 3단계를 뒤져야 함)
- 깊이 제한 없음 → 소금빵 → 소금빵반죽 → 묵은반죽 자동 처리

### 5.2 소금빵 전체 계산 예시 (검산용)

구매 단가 가정: 밀가루 1.5원/g, 물 0원/g, 소금 2원/g, 이스트 20원/g, 버터 15원/g, 소금버터 18원/g

**① 묵은반죽** (밀가루 1000g 기준)
```
밀가루 1000g × 1.5 = 1,500원
물      700g × 0   =     0원
소금     20g × 2   =    40원
─────────────────────────────
원가 합계 1,540원 ÷ 산출 1,720g = 0.895원/g
```

**② 소금빵 반죽** (밀가루 1000g 기준, 묵은반죽 20%)
```
밀가루   1000g × 1.5   = 1,500원
물        650g × 0     =     0원
소금       20g × 2     =    40원
이스트     10g × 20    =   200원
버터       50g × 15    =   750원
묵은반죽  200g × 0.895 =   179원   ← ★ 재귀 롤업 결과
──────────────────────────────────
원가 합계 2,669원 ÷ 산출 1,930g = 1.383원/g
```

**③ 소금빵 1개**
```
소금빵 반죽  60g × 1.383 = 82.98원
소금버터     12g × 18    = 216.00원
소금          1g × 2     =   2.00원
──────────────────────────────────
1개당 원가 = 300.98원 → 301원
판매가 3,000원이면 마진 2,699원 (마진율 90%)
```

성형 로스가 있다면(반죽 라인 `loss_pct` 5%): `82.98 / 0.95 = 87.35원` → 1개당 305원.

### 5.3 계산기(베이커스 퍼센트)와 공유

계산기의 기존 공식은 그대로다 — 데이터 소스만 jsonb에서 `bom_lines`로 바뀐다.

| 기존 | 이후 |
|---|---|
| `state.flours[]` | `bom_lines` 중 `is_flour = true` |
| `state.ingredients[]` | `bom_lines` 중 `is_flour = false` |
| `ing.pct` | `line.qty` (`qty_unit='pct'`) |
| `ing.name` | `line.display_name` 또는 `child.name` |
| `ing.ingredientId` | `line.child_item_id` |
| `ing.stage` | `line.stage` |

`computeFlourWeight()` / `pctFromWeight()` / `roundPctPrecise()` 등 계산 로직은 **변경 없음**.

---

## 6. UI 재설계

### 6.1 화면 구조: 3개 → 1개

```
현재:  [재료 관리]  [원가 계산 ─ 레시피 탭 | 조합 제품 탭]
                     ↑ 개념 4개, 화면 3개, "어디서 만들지" 판단 필요

이후:  [품목]
        └ 필터 칩: 전체 / 구매 재료 / 반죽·반제품 / 판매 제품
        └ 목록 → 클릭 → 그 품목의 BOM 트리 + 원가
```

하단 네비게이션 탭은 그대로 4개를 유지하되 의미가 바뀐다:
`레시피(계산기) / 아카이브 / 품목 / 원가`
→ 또는 **품목·원가를 하나로 합쳐 3탭**으로 줄이는 것도 가능 (§9 Q5)

### 6.2 품목 상세 — BOM 트리 뷰

핵심은 **한 화면에서 전체 계층이 보이는 것**이다. 지금은 소금빵 원가를 확인하려면
조합 제품 화면 → 반죽 원가 궁금하면 레시피 탭으로 이동 → 묵은반죽은 아예 볼 수 없다.

```
┌────────────────────────────────────────────────┐
│ ← 소금빵                        [판매 제품]     │
│                                                │
│   1개당 원가  301원                             │
│   판매가     3,000원                            │
│   마진       2,699원 (90%)                      │
│                                                │
│   구성                                          │
│   ▼ 소금빵 반죽          60g      83원   [편집] │
│      ├ 밀가루           100%      90원         │
│      ├ 물                65%       0원         │
│      ├ 소금               2%       1원         │
│      ├ 이스트             1%       6원         │
│      ├ 버터               5%      23원         │
│      └ ▼ 묵은반죽        20%       6원         │
│           ├ 밀가루      100%                   │
│           ├ 물           70%                   │
│           └ 소금          2%                   │
│     소금버터             12g     216원          │
│     소금                  1g       2원          │
│                                                │
│   + 구성 추가                                   │
└────────────────────────────────────────────────┘
```

- **접기/펼치기**로 깊이를 조절 (기본은 1단계만 펼침)
- 각 줄에 그 줄의 **원가 기여액**이 바로 보임 → 어디가 비싼지 즉시 파악
- 단가가 빠진 품목은 그 줄에 빨간 표시 + 탭하면 바로 그 품목 편집으로 이동
  (지금은 "일부 재료 단가가 없어요"라는 문구만 뜨고 어느 것인지 찾아 헤매야 함)
- 기존 `.acc-row` 아코디언 패턴을 그대로 재사용 — 새 시각 언어를 만들지 않는다

### 6.3 "구성 추가"의 동작 — 실패한 두 시도와 무엇이 다른가

이번에는 **재료를 추가하는 것과 하위 반죽을 추가하는 것이 같은 동작**이다.

```
[+ 구성 추가] → 검색창 하나
                 "소금" 입력 →  소금            [구매 재료]
                                소금빵 반죽      [반죽]
                                토핑용 소금      [구매 재료]
                                + "소금" 새 재료로 등록
```

- 별도의 "다른 레시피 연결" 링크가 **없다** (1차 실패 원인 제거)
- 별도의 "가져오기" 버튼이 **없다** (2차 실패 원인 제거)
- 구매 재료든 반죽이든 **같은 검색창에서 같은 방식으로** 고른다
- 없는 이름이면 그 자리에서 새 구매 품목으로 등록 (지금 `attachIngredientLinker()`의 "+ 새 재료로 추가"와 동일)

두 번의 실패는 "중첩이 특별한 기능"이라서 특별한 UI가 필요했기 때문이다.
BOM에서는 중첩이 기본이므로 **특별한 UI 자체가 없어진다.**

### 6.4 계산기 화면의 변화

가능한 한 **지금 그대로 유지**한다. 사용자가 매일 쓰는 화면이고, 계산 로직도 그대로다.

바뀌는 것은 재료 행 하나뿐:

```
현재:  [버터            ] [ 5 ]%  [  50 ]g   ▲▼ ×
       ← 자유 텍스트. 원가 계산 화면에서 따로 연결해야 함

이후:  [버터            ] [ 5 ]%  [  50 ]g   ▲▼ ×
       ← 품목 참조. 타이핑하면 자동완성이 뜨고,
         없는 이름이면 그대로 새 품목이 됨 (입력 흐름은 동일)
```

**타이핑을 멈추게 하지 않는 것이 조건이다.** 자동완성은 제안만 하고,
무시하고 계속 타이핑하면 새 품목으로 등록된다 — 지금의 자유 텍스트 입력감과 동일하게 유지한다.
(2026-07-24에 인라인 콤보를 걷어낸 이유가 정확히 이 마찰이었으므로, 같은 실수를 반복하지 않는다.)

하위 반죽이 연결된 행은 이름 옆에 작은 표식만 붙이고, 탭하면 품목 상세로 이동한다.
**계산기 안에서 하위 트리를 펼치지 않는다** — 그게 1차 시도의 실패 지점이었다.

---

## 7. 마이그레이션

기존 데이터는 **한 건도 잃지 않고** 자동 변환할 수 있다.

### 7.1 매핑

| 기존 | 이후 |
|---|---|
| `ingredients` 1행 | `items` 1행 (`kind='purchased'`), `category`/`is_active`/단가 그대로 |
| `recipes` 1행 | `items` 1행 (`kind='made'`, `recipe_id`=원본, `formula_mode='bakers_pct'`) + `recipes` 행 유지 |
| `recipes.flours[]` 항목 | `bom_lines` 1행 (`qty_unit='pct'`, `is_flour=true`, `child_item_id`=`ingredientId`) |
| `recipes.ingredients[]` 항목 | `bom_lines` 1행 (`qty_unit='pct'` 또는 `'g'`, `is_flour=false`) |
| `products` 1행 | `items` 1행 (`kind='made'`, `is_sellable=true`, `selling_price`) + `bom_lines` 1행(자식=그 레시피의 item, `qty`=`portion_weight`, `loss_pct`=`loss_rate_pct`) |
| `composite_products` 1행 | `items` 1행 (`kind='made'`, `is_sellable=true`) |
| `composite_product_components` 1행 | `bom_lines` 1행 (`recipe_id`/`ingredient_id` → 해당 `child_item_id`) |
| `cost_snapshot_*` 3개 컬럼 | **버림** (하나의 진실이 되므로 불필요) |

### 7.2 `ingredientId`가 null인 행 (미연결 재료)

현재 상당수 행이 이 상태다 (이름만 있고 원가 연결 안 됨).

**변환 규칙**: 이름을 `normalizeForMatch()`(기존 함수 재사용)로 정규화해 매칭 시도
→ 매칭되면 그 품목에 연결, 안 되면 **그 이름으로 구매 품목을 새로 만들되 단가는 0**.

결과적으로 지금 "단가 없음"이던 재료들이 **품목 목록에 이름과 함께 나타난다.**
사용자는 목록에서 단가만 채우면 된다 — 지금처럼 레시피를 하나씩 열어 연결할 필요가 없다.
(이게 §2.5 이중 입력 문제의 실질적 해소다.)

### 7.3 안전장치

- 마이그레이션은 **한 번만 실행**되고, 기존 테이블은 **지우지 않는다** (읽기 전용으로 남김)
- 변환 후 검증: 모든 레시피의 g당 원가를 신·구 엔진으로 각각 계산해 **오차 0.01원 이내인지 대조**
- 불일치가 나오면 그 레시피 목록을 보여주고 사용자가 확인할 수 있게 함
- 구 테이블 삭제는 **한참 뒤 별도 단계**로 (§8 5단계)

---

## 8. 단계별 실행 계획

각 단계가 끝난 시점에 앱이 **정상 동작해야 한다.** 중간에 깨진 상태로 두지 않는다.

### 0단계 — 결정 (코드 작성 전)
§9의 열린 질문을 확정한다. 특히 Q1(계산기 통합 범위)과 Q3(공유 시 하위 품목 노출)은
스키마와 RLS를 좌우하므로 반드시 먼저 답이 나와야 한다.

### 1단계 — 스키마 + 마이그레이션 (사용자 눈에 안 보임)
- `items` / `bom_lines` 테이블, RLS, 순환 방지 트리거 생성
- 기존 데이터 변환 스크립트 실행 + 신·구 원가 대조 검증
- **이 시점에 UI는 아무것도 안 바뀐다** — 기존 화면이 계속 구 테이블을 쓴다
- 롤백 가능: 새 테이블만 지우면 됨

### 2단계 — 원가 롤업 엔진 (사용자 눈에 안 보임)
- `rollupCost()` 구현 + 기존 3개 계산 경로와 결과 대조
- 기존 화면은 여전히 구 로직 사용, 새 엔진은 병행 검증만
- 소금빵 예시(§5.2)를 검증 케이스로 사용

### 3단계 — 품목 화면 신설
- 새 "품목" 서브페이지 + BOM 트리 뷰 (§6.2)
- 재료 관리 / 원가 계산 화면은 **아직 남겨둔다** (병행 기간)
- 이 시점부터 사용자가 소금빵 같은 중첩 구조를 실제로 만들 수 있다 ← **첫 실사용 가치**

### 4단계 — 계산기를 `bom_lines` 기반으로 전환
- 계산기가 jsonb 대신 `bom_lines`를 읽고 쓰도록 교체
- 재료 행 자동완성 (§6.4)
- CSV 내보내기/가져오기를 `bom_lines` 기준으로 재작성
- 공유/아카이브 RLS 확장
- **가장 위험한 단계** — 매일 쓰는 화면이므로 이전 단계들이 충분히 안정된 뒤에 진행

### 5단계 — 정리
- 구 화면(재료 관리 / 원가 계산 두 탭) 제거
- `cost_snapshot_*` 컬럼 제거
- 구 테이블(`ingredients` / `products` / `composite_products` / `composite_product_components`) 제거
- 도움말 페이지 갱신

**3단계까지만 해도 소금빵 문제는 풀린다.** 4·5단계는 이중 입력 해소와 정리를 위한 것이므로,
3단계에서 멈추고 실사용 반응을 본 뒤 진행 여부를 정해도 된다.

---

## 9. 열린 질문 (착수 전 결정 필요)

**Q1. 계산기를 `bom_lines` 기반으로 완전히 전환할 것인가?** (§4.4, 8-4단계)
- (A) 전환한다 — 이중 입력·스냅샷 복잡도가 근본적으로 사라짐. 대신 공사가 크고 CSV/공유 재작성 필요.
- (B) 계산기는 jsonb 그대로 두고 BOM은 원가 전용 레이어로만 — 공사는 작지만 이중 입력이 남음.
- 권장: **(A)**. (B)는 지금 구조의 문제를 그대로 안고 가는 것이라 "아예 리뉴얼"이라는 요청에 못 미친다.

**Q2. 판매 제품과 반죽을 목록에서 구분할 것인가?**
- 데이터상 둘 다 제조 품목이지만, 사용자 입장에선 "파는 것"과 "만드는 중간재"가 다르게 느껴질 수 있다.
- 필터 칩으로 나누는 것으로 충분한지, 아니면 목록 자체를 분리할지.

**Q3. 공유/아카이브로 레시피를 받은 사람에게 하위 품목까지 보여줄 것인가?**
- 소금빵반죽을 공유받으면 묵은반죽 배합도 같이 보이는가?
- (A) 보인다 — 안 그러면 레시피가 불완전해서 재현 불가
- (B) 안 보인다 — 하위 배합은 영업 비밀일 수 있음
- 원가(단가)는 **어느 쪽이든 공유하지 않는다** (지금도 `products`가 owner-only인 것과 동일 원칙)

**Q4. `'ea'` 단위 품목의 원가를 어디까지 지원할 것인가?**
- `unit_weight_g`(1개 무게)를 입력하면 g 환산이 되는데, 이걸 필수로 요구할지 선택으로 둘지.
- 선택으로 두면 "개당으로만 계산되는 품목"이 생겨 g 기반 배합에 섞을 수 없다.

**Q5. 하단 탭 구성을 어떻게 할 것인가?**
- 현재 4탭: 레시피 / 아카이브 / 재료 / 원가
- 품목 화면이 재료+원가를 흡수하면 3탭(레시피 / 아카이브 / 품목)이 된다
- 원가를 별도 탭으로 남길지(판매 제품 원가 요약 전용), 품목 안에 통합할지

**Q6. `account.html` 파일 분리를 이 기회에 할 것인가?**
- 현재 7,004줄. BOM 레이어를 얹으면 8,000줄을 넘어선다.
- "외부 의존성 없음" 원칙은 지키되, `<script type="module">` + 파일 분리는 의존성 추가 없이 가능하다.
- 다만 배포가 `mino-account`에 파일 하나 복사하는 방식이라 배포 절차도 같이 바뀐다.

---

## 10. 범위 밖 (이번에 하지 않을 것)

- **생산 계획 / 발주** — `2026-07-14_재료비생산관리_스펙.md`의 나머지 절반. BOM이 갖춰지면
  자연스럽게 얹을 수 있지만(BOM 전개 = 소요량 계산), 이번 범위는 원가까지다.
- **재고 관리** — 입출고, 현재고. BOM과 별개 축이다.
- **가격 이력 추적** — 재료 단가가 언제 얼마였는지. 지금도 없고 이번에도 안 한다.
  (CSV 가져오기의 "기존 25,000원 → 새값 30,000원" 확인 단계가 사실상의 대체 수단)
- **원가 확정(freeze) / 버전 관리** — `cost_snapshot_*`를 없애는 대신 "이 시점 원가를 박제"하는
  기능이 필요할 수 있으나, 요청된 적 없으므로 제외. 필요해지면 `items`에 스냅샷 테이블을 따로 붙이면 된다.
- **`index.html` / `manager.html`** — 로그인·DB 개념 자체가 없는 파일들이다. 전혀 건드리지 않는다.

---

## 11. 요약

| | 현재 | BOM 이후 |
|---|---|---|
| 개념 수 | 4 (재료/레시피/제품/조합제품) | 1 (품목) + 1 관계 (BOM 라인) |
| 화면 수 | 3 (재료 관리 / 원가-레시피 / 원가-조합) | 1 (품목) |
| 중첩 깊이 | 2단계 고정 | 무제한 |
| 소금빵 표현 | **불가** (묵은반죽) | 가능 |
| 원가 계산 코드 | 3벌 | 1벌 (`rollupCost`) |
| 재료 연결 | 이중 입력 (계산기 + 원가 화면) | 1회 (계산기에서) |
| 스냅샷 동기화 | 수동 "다시 불러오기" | 불필요 |
| 단가 없음 안내 | "일부 재료가…" (어느 것인지 모름) | 품목명으로 지목 |
| `'ea'` 단위 원가 | 항상 0 | 정상 계산 |
| 제조 로스(수율) | 표현 불가 | `process_loss_pct` |

**가장 중요한 한 가지**: 중첩을 "특별한 기능"으로 얹는 대신 **기본 구조로 삼는다.**
2026-07-28의 두 번의 실패는 UI 문제가 아니라 모델 문제였다.
