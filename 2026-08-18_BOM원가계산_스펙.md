# 2026-08-18 원가 계산 / 재료 관리 BOM 전면 리뉴얼 스펙

> **개정 2판.** 초판(같은 날 오전)에 대한 8가지 피드백을 반영해 설계를 상당 부분 바꿨다.
> 특히 **묵은반죽 같은 하위 배합의 재귀 중첩을 기본 기능에서 뺐고**(§3.3),
> **계산기 화면을 건드리지 않는 방향**으로 전환 전략을 바꿨으며(§6.4),
> **재료 가격 이력 추적을 범위 안으로 가져왔다**(§7).
>
> 이 문서는 **설계 스펙**(`_스펙`)이지 작업기록이 아니다. 아직 아무것도 구현되지 않았다.

---

## 0. 결정 요약 (초판 대비 변경점)

| # | 결정 | 초판 | 2판 |
|---|---|---|---|
| 1 | 전환 리스크 분석 | 없음 | §10 신설 — 혼란 요소 7가지와 완화책 |
| 2 | 외부 의존성 방침 | "없음" 고수 | **해제** (`account.html` 한정, §11) |
| 3 | 재료 가격 이력 | 범위 밖 | **범위 안** — §7 신설 |
| 4 | 하위 배합 중첩(묵은반죽) | 재귀 롤업 핵심 기능 | **수동 단가 입력이 기본**, 재귀는 옵션 (§3.3) |
| 5 | 계산기 전환 | `bom_lines` 직접 읽기/쓰기 | **화면 무변경 + 저장 시 자동 미러링** (§6.4) |
| 6 | 판매 제품 / 반죽 구분 | 미정 | **필터 칩으로 분리** (§6.2) |
| 7 | 공유 시 하위 품목 노출 | 미정 | **안 보여준다** (§6.5) |
| 8 | `'ea'` 단위 | 미정 | **확정** — 구매 기준/사용 기준 무게 두 칸 (§12) |
| 9 | 완제품을 개수로 세기 | 범위 밖(제안) | **범위 밖 확정** — 생산 계획 영역 (§12-7) |

---

## 1. 배경 — 왜 지금 구조로는 안 되는가

사용자 원문:

> 원가계산이 너무 불편함. BOM 개념으로 접근하면 좋겠음.
> 예) 소금빵의 경우 소금빵반죽 60g + 충전용 소금버터 12g + 토핑용 소금 1g으로 만들어짐.
> 나는 기존의 원가계산, 재료관리를 아예 리뉴얼했으면 좋겠음.

```
소금빵 (판매 제품, 1개 = 73g)
├─ 소금빵 반죽        60g   ← 배합(레시피)
├─ 충전용 소금버터    12g   ← 구매 재료
└─ 토핑용 소금         1g   ← 구매 재료
```

**초판과 달라진 전제**: 초판은 여기에 "소금빵반죽 안에 묵은반죽이 들어간다"를 더해
**무한 재귀 중첩**을 설계의 중심에 뒀다. 피드백 4번으로 이 요구가 철회됐다 —
묵은반죽은 그냥 **단가를 직접 넣는 품목**으로 두기로 했다.

그래서 이 리뉴얼의 목적이 바뀌었다:

- ~~중첩을 지원하는 것~~ → **개념·화면·계산 로직의 중복을 없애는 것**

중첩이 빠져도 리뉴얼의 근거는 그대로 남는다 — 오히려 실제 불편의 대부분이 중첩이 아니라
**같은 일을 4개 테이블 / 3개 화면 / 3벌의 계산 코드로 나눠 하고 있다는 것**에서 온다(§2).

---

## 2. 현재 구조 상세 분석

### 2.1 개념이 4개 테이블로 쪼개져 있다

| 테이블 | 역할 | 정의 위치 |
|---|---|---|
| `ingredients` | 구매 재료 마스터 → `price_per_gram` | `supabase/schema.sql:67` |
| `recipes` | 배합(비율). `flours`/`ingredients` jsonb | `supabase/schema.sql:11` |
| `products` | 레시피 **1개**에 종속된 판매 단위 | `supabase/schema.sql:86` |
| `composite_products` + `..._components` | 여러 레시피/재료를 조합한 판매 단위 | `supabase/schema.sql:103`, `:118` |

### 2.2 "제품"이 두 개의 별개 개념이다

같은 "제품"인데 등록하는 곳이 다르다.

- 레시피 1개 기반 → 원가 계산 탭 안의 "제품 & 원가" (`renderProductsList()`, `account.html:3771`)
- 여러 개 기반 → 별도 "조합 제품" 탭 (`renderCompositeProductsList()`, `account.html:4145`)

사용자는 소금빵을 만들려다 **"이건 제품인가 조합 제품인가"부터 판단**해야 하고,
나중에 구성이 바뀌면(반죽 하나였는데 토핑이 추가되면) 한쪽에서 지우고 다른 쪽에서 다시 만들어야 한다.

### 2.3 원가 계산 로직이 세 벌로 흩어져 있다

| 함수 | 위치 | 하는 일 |
|---|---|---|
| `computeCostBreakdown()` | `account.html:2908` | 레시피 g당 원가 + 재료별 기여도 |
| `refreshProductCosts()` | `account.html:3739` | 제품 행별 원가/마진 |
| `refreshCompositeCost()` | `account.html:4273` | 조합 제품 구성요소별 합산 |

**이게 실제 버그를 낳았다.** 2026-08-18에 발견·수정한
"조합 제품 원가가 항상 0원" 버그(`2026-08-18_조합제품원가버그_작업기록.md`)가 정확히 이 구조 때문이다 —
2026-07-25 스냅샷 전환에서 세 경로 중 하나(`costPerGramForRecipeId()`)만 옛 소스를 계속 읽고 있었다.
**계산 경로가 하나였다면 생길 수 없는 버그였다.**

### 2.4 원가 스냅샷(`cost_snapshot_*`)이 복잡도의 큰 원인

- `recipes.cost_snapshot_flours` / `cost_snapshot_ingredients` / `cost_snapshot_taken_at`
- 처음 열 때 자동 스냅샷 (`openCostCalcRecipe()`, `account.html:6267`)
- 원본이 바뀌면 "⚠ 원본 수정됨" 배너 → 사용자가 **수동으로** "다시 불러오기" (`reloadCostSnapshot()`)

도입 취지("레시피 편집이 원가를 조용히 바꾸면 안 된다")는 타당했지만,
**사용자가 동기화를 손으로 관리해야 한다**는 부담이 남았다. §6.4의 자동 미러링이 이를 대체한다.

### 2.5 이름과 원가 연결이 이중 입력이다

2026-07-24에 의도적으로 분리한 구조다 — 그 결정 자체는 타당했다:

> 레시피는 "버터"라고 짧게 쓰는데 실제 구매품은 "앵커버터"다.
> 레시피 쓰면서 매번 DB를 매칭하는 건 마찰이고, 고르면 이름이 브랜드명으로 덮어써진다.

그래서 지금은 **계산기에서 "버터"를 타이핑하고, 원가 계산 화면에서 그 행을 "앵커버터"에 다시 연결**한다.
이름을 지키려다 입력을 두 번 하게 됐다. 레시피가 20개면 연결 작업도 20번이다.

### 2.6 그 밖의 구조적 결함

- **`'ea'`(개수) 재료는 원가가 항상 0** — `computePricePerGram()`(`account.html:2886`)이 `'ea'`면 `0`을 반환.
  계란을 개수로 사면 원가에서 통째로 빠진다. (§12)
- **제조 로스(수율)를 표현할 곳이 없다** — 지금의 `loss_rate_pct`는 전부 *성형/재단* 로스다.
  믹싱 손실, 발효 감량을 넣을 자리가 없다.
- **CSV 가져오기가 가격을 덮어쓴다** — 이전 가격이 사라져 변화 추적이 불가능하다. (§7)
- **파일 크기** — `account.html`이 7,000줄, 함수 206개.

---

## 3. BOM 재설계 — 핵심 개념

### 3.1 하나의 개념: "품목(Item)"

지금의 4개 개념(재료 / 레시피 / 제품 / 조합 제품)을 **품목 하나**로 통합한다.

```
품목(Item)
├─ 구매 품목 (purchased) — 사서 쓰는 것. 구매량/가격 입력.
└─ 제조 품목 (made)      — 만들어 쓰는 것. 하위 구성(BOM)을 가짐.
```

"반죽"과 "완제품"을 데이터상 구분하지 않는다 — 둘 다 제조 품목이고,
차이는 `is_sellable`(판매가를 갖는가) 플래그 하나뿐이다.
**단, 사용자 화면에서는 필터 칩으로 확실히 나눈다**(§6.2, 피드백 6번).

### 3.2 하나의 관계: "BOM 라인"

```
BOM 라인 = (상위 품목, 하위 품목, 수량, 단위, 로스율)
```

| 상위 | 하위 | 수량 |
|---|---|---|
| 소금빵 | 소금빵 반죽 | 60g |
| 소금빵 | 소금버터 | 12g |
| 소금빵 | 소금 | 1g |
| 소금빵 반죽 | 밀가루 | 100% |
| 소금빵 반죽 | 묵은반죽 | 20% |

BOM 라인의 단위는 3종이라 베이커스 퍼센트와 충돌하지 않는다:
`pct`(밀가루 100% 기준) / `g`(절대 중량) / `ea`(개수).

### 3.3 ★ 하위 배합의 재귀 중첩은 기본에서 뺀다 (피드백 4번)

> "묵은반죽 표현은 없애자. 그냥 사용자가 묵은반죽 가격 0이나 원하는 가격 넣고 원가 계산하자."

이 결정으로 설계가 크게 단순해진다. 품목마다 **원가 산정 방식**을 고를 수 있게 한다:

```
items.cost_mode = 'manual' | 'auto'
```

- **`manual`(기본값)** — 단가를 사용자가 직접 입력한다(`manual_cost_per_gram`).
  구매 품목이든, 묵은반죽처럼 "만들긴 하지만 원가는 대충 잡고 넘어갈" 품목이든 여기 해당.
- **`auto`** — 하위 BOM 라인에서 계산한다. 소금빵처럼 구성이 명확한 판매 제품에 쓴다.

**묵은반죽은 `manual`로 두고 단가 0(또는 원하는 값)을 넣으면 끝이다.**
하위 배합을 등록할 필요도, 연결할 필요도 없다.

**재귀 롤업 코드와 순환 방지 트리거는 그래도 남긴다** — 이유:
1. `auto` 품목이 다른 `auto` 품목을 포함하는 순간(소금빵이 여러 개 들어가는 세트 상품 등)
   재귀가 실제로 필요해진다. 깊이 제한을 두면 나중에 스키마를 다시 바꿔야 한다.
2. 구현 비용 차이가 작다 — 재귀 함수 하나 + `visiting` 집합 + DB 트리거 하나.
3. `manual`을 만나는 순간 재귀가 **멈춘다**(잎으로 취급). 즉 지금 사용 방식에서는
   실질적으로 깊이 1~2단계만 돌게 되고, 성능·복잡도 부담이 없다.

**UI에서는 중첩을 권하지 않는다.** "하위 배합 연결" 같은 버튼을 만들지 않는다 —
2026-07-28에 두 번 시도했다가 두 번 다 걷어낸 그 UI다. 품목을 `auto`로 바꾸는 것은
품목 상세의 설정 토글 하나로만 가능하게 두고, 기본은 `manual`이다.

### 3.4 산출량(yield)과 로스 두 종류

```
제조 품목의 단위 원가 = (하위 품목 원가 합계) ÷ (실제 산출량)
실제 산출량 = 배합 산출 무게 × (1 − 제조 로스율)
```

| 종류 | 저장 위치 | 의미 | 예 |
|---|---|---|---|
| **제조 로스** | `items.process_loss_pct` | 이 품목을 만들 때의 손실 | 믹싱볼 잔량, 발효 감량 |
| **투입 로스** | `bom_lines.loss_pct` | 상위에 넣을 때의 손실 | 성형 파지, 재단 손실 |

현재 `products.loss_rate_pct` / `composite_product_components.loss_rate_pct`는 전부 **투입 로스**이므로
`bom_lines.loss_pct`로 그대로 이관된다(기존 `effectiveCostPerGram()` 공식 재사용).

`batch_yield_g`를 비워두면 하위 라인 무게 합계로 자동 계산 → **지금 동작과 동일**하므로
마이그레이션이 무손실이고, 사용자가 새 칸을 채울 의무도 없다.

---

## 4. 데이터 모델

### 4.1 `items` — 통합 품목 마스터

```sql
create table items (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,

  name text not null,                          -- 레시피에서 부르는 이름 ("버터")
  kind text not null default 'purchased',      -- 'purchased' | 'made'
  category text not null default '',           -- 자유 텍스트 (기존 ingredients.category 계승)
  is_active boolean not null default true,     -- 소프트 삭제 (기존 정책 계승)

  -- 원가 산정 방식 (§3.3) ---------------------------------------------------
  cost_mode text not null default 'manual',    -- 'manual' | 'auto'
  manual_cost_per_gram numeric not null default 0,  -- cost_mode='manual'일 때 쓰는 값
  manual_cost_per_ea numeric not null default 0,

  -- kind='purchased'의 구매 정보 (manual 단가를 여기서 자동 계산) ------------
  supplier_item_name text not null default '', -- 실제 구매품명 ("앵커버터")
  purchase_amount numeric not null default 0,
  purchase_unit text not null default 'g',     -- 'g'|'kg'|'ml'|'l'|'ea'
  purchase_price numeric not null default 0,
  unit_weight_g numeric,                       -- 1개 무게(구매 기준, 껍질 포함) — 계란이면 60 (§12)
  usable_weight_g numeric,                     -- 실제 쓰는 무게(손질 후) — 계란이면 50.
                                               -- null이면 unit_weight_g와 같다고 본다

  -- kind='made' 전용 -------------------------------------------------------
  recipe_id uuid references recipes(id) on delete set null,  -- 배합은 계산기에서 (§6.4)
  batch_yield_g numeric,                       -- null이면 하위 라인 무게 합계로 자동
  process_loss_pct numeric not null default 0,

  -- 판매 품목일 때 ---------------------------------------------------------
  is_sellable boolean not null default false,
  selling_price numeric not null default 0,

  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

**`name` vs `supplier_item_name` 분리가 §2.5의 이중 입력 문제를 푼다.**
레시피에는 "버터"로 뜨고, 품목 관리에는 "버터 → 앵커버터 10kg 100,000원"으로 관리된다.
브랜드명이 레시피 표시를 덮어쓰지 않으므로 **연결을 별도 화면으로 분리할 이유가 사라진다.**

### 4.2 `bom_lines` — 품목 간 포함 관계

```sql
create table bom_lines (
  id uuid primary key default gen_random_uuid(),
  parent_item_id uuid not null references items(id) on delete cascade,
  child_item_id  uuid references items(id) on delete restrict,  -- 참조 중이면 삭제 차단

  display_name text not null default '',  -- 이 배합에서만 쓰는 표시명 (비우면 child.name)
  qty numeric not null default 0,
  qty_unit text not null default 'g',     -- 'pct' | 'g' | 'ea'
  loss_pct numeric not null default 0,    -- 투입 로스
  is_flour boolean not null default false,-- qty_unit='pct'일 때 100% 기준 밀가루인지
  stage text not null default 'final',    -- 'pre'(사전발효) | 'final' — 기존 풀리시 개념 계승
  sort_order integer not null default 0,

  created_at timestamptz not null default now()
);
create index bom_lines_parent_idx on bom_lines(parent_item_id);
create index bom_lines_child_idx  on bom_lines(child_item_id);
```

- `on delete restrict`: 어딘가에 쓰이는 품목을 지우면 원가가 조용히 틀어진다.
  기존 `is_active` 소프트 삭제 정책과 같은 취지를 DB가 강제한다. (§10-4에 사용자 영향 정리)
- `is_flour`가 기존 `state.flours` / `state.ingredients` 두 배열 분리를 대체한다.
- `display_name`은 "이 배합에서만 다르게 부르고 싶을 때"의 탈출구다(같은 소금을 "토핑용 소금"으로).

### 4.3 순환 참조 방지

`cost_mode='auto'`가 서로를 물면 무한 재귀가 된다. **DB 트리거로 막는다**
(클라이언트 검사만으로는 부족 — 여러 탭/기기에서 동시 편집 가능):

```sql
create or replace function check_bom_cycle()
returns trigger language plpgsql as $$
begin
  if new.child_item_id is null then return new; end if;
  if new.child_item_id = new.parent_item_id then
    raise exception '품목은 자기 자신을 포함할 수 없어요';
  end if;
  if exists (
    with recursive descendants as (
      select child_item_id as id from bom_lines where parent_item_id = new.child_item_id
      union
      select b.child_item_id from bom_lines b
        join descendants d on b.parent_item_id = d.id
    )
    select 1 from descendants where id = new.parent_item_id
  ) then
    raise exception '순환 참조가 생겨요 — 이 품목은 상위 품목 안에 이미 포함돼 있어요';
  end if;
  return new;
end; $$;

create trigger bom_lines_no_cycle before insert or update on bom_lines
  for each row execute function check_bom_cycle();
```

---

## 5. 원가 롤업 엔진

세 곳에 흩어진 계산(§2.3)을 **함수 하나**로 통일한다.

```js
// 반환: { costPerGram, costPerEa, unpriced: [품목명...], yieldG }
function rollupCost(itemId, memo, visiting) {
  if (visiting.has(itemId)) throw new CycleError(itemId);   // DB 트리거의 2차 방어선
  if (memo[itemId]) return memo[itemId];                    // 같은 품목이 여러 번 쓰여도 1회만

  const item = itemsById[itemId];

  // ★ manual이면 여기서 멈춘다 — 잎으로 취급. 묵은반죽이 여기 해당(§3.3).
  if (item.cost_mode === 'manual') {
    const priced = item.manual_cost_per_gram > 0 || item.manual_cost_per_ea > 0;
    return memo[itemId] = {
      costPerGram: item.manual_cost_per_gram,
      costPerEa:   item.manual_cost_per_ea,
      // 단가 0을 "의도적으로 0"과 "아직 안 넣음"으로 구분해야 한다 — §10-6 참고
      unpriced:    priced ? [] : [item.name],
      yieldG:      null
    };
  }

  // auto: 하위를 재귀 롤업해 합산
  visiting.add(itemId);
  const lines = bomLinesByParent[itemId] || [];
  const flourG = totalFlourGrams(lines);   // qty_unit='pct' 라인의 기준 무게

  let totalCost = 0, totalWeight = 0, unpriced = [];
  for (const line of lines) {
    const child = rollupCost(line.child_item_id, memo, visiting);
    const g = toGrams(line, flourG, child);            // pct/g/ea → g 환산
    const lineCost = costOfLine(line, child, g);
    // 투입 로스: L%가 버려지면 1g 쓰려고 1/(1-L) g을 투입해야 함 (기존 공식과 동일)
    const loss = clamp(line.loss_pct, 0, 99) / 100;
    totalCost   += loss > 0 ? lineCost / (1 - loss) : lineCost;
    totalWeight += g;
    unpriced.push(...child.unpriced);
  }
  visiting.delete(itemId);

  const batchG = item.batch_yield_g ?? totalWeight;
  const yieldG = batchG * (1 - clamp(item.process_loss_pct, 0, 99) / 100);

  return memo[itemId] = {
    costPerGram: yieldG > 0 ? totalCost / yieldG : 0,
    costPerEa: null, unpriced, yieldG
  };
}
```

**핵심 성질:**
- `manual`에서 재귀가 멈추므로, 지금 사용 방식(묵은반죽 수동)에서는 실질 깊이가 1~2단계다.
- 메모이제이션 덕에 같은 품목이 여러 번 나와도 한 번만 계산.
- `unpriced`가 **어느 품목의 단가가 비었는지 이름으로 알려준다**
  (지금은 "일부 재료 단가가 없어요"만 뜨고 어느 것인지 뒤져야 함).

### 5.1 소금빵 검산 (묵은반죽 = 수동 단가 0.9원/g으로 입력한 경우)

구매 단가: 밀가루 1.5원/g, 물 0원/g, 소금 2원/g, 이스트 20원/g, 버터 15원/g, 소금버터 18원/g

**① 소금빵 반죽** (`auto`, 밀가루 1000g 기준)
```
밀가루   1000g × 1.5  = 1,500원
물        650g × 0    =     0원
소금       20g × 2    =    40원
이스트     10g × 20   =   200원
버터       50g × 15   =   750원
묵은반죽  200g × 0.9  =   180원   ← manual 단가, 재귀 없이 그대로 사용
───────────────────────────────────
합계 2,670원 ÷ 산출 1,930g = 1.383원/g
```

**② 소금빵 1개** (`auto`, 판매 제품)
```
소금빵 반죽  60g × 1.383 =  82.98원
소금버터     12g × 18    = 216.00원
소금          1g × 2     =   2.00원
───────────────────────────────────
1개당 원가 301원 · 판매가 3,000원 → 마진 2,699원 (90%)
```

성형 로스 5%를 반죽 라인에 넣으면: `82.98 / 0.95 = 87.35원` → 1개당 305원.

---

## 6. UI 재설계

### 6.1 화면 구조: 3개 → 1개

```
현재:  [재료 관리]  [원가 계산 ─ 레시피 탭 | 조합 제품 탭]
이후:  [품목]  ← 필터 칩으로 종류 구분
```

### 6.2 필터 칩으로 판매 제품과 반죽을 나눈다 (피드백 6번)

데이터상으로는 둘 다 제조 품목이지만, 사용자 멘탈 모델에서는 완전히 다르다.

```
[전체]  [구매 재료]  [반죽·반제품]  [판매 제품]
```

- **구매 재료** — `kind='purchased'` (지금의 재료 관리에 있던 것들)
- **반죽·반제품** — `kind='made'` && `is_sellable=false`
- **판매 제품** — `is_sellable=true`

기본 진입 시 어떤 칩을 켜둘지는 사용 빈도를 봐야 하지만,
**"구매 재료"를 기본으로** 두는 것이 지금의 "재료 관리"와 가장 가까워 이질감이 적다(§10-1).

### 6.3 품목 상세

```
┌────────────────────────────────────────────────┐
│ ← 소금빵                        [판매 제품]     │
│                                                │
│   1개당 원가  301원                             │
│   판매가     3,000원                            │
│   마진       2,699원 (90%)                      │
│                                                │
│   구성                                          │
│     소금빵 반죽          60g      83원          │
│     소금버터             12g     216원          │
│     소금                  1g       2원          │
│                                                │
│   + 구성 추가                                   │
└────────────────────────────────────────────────┘
```

- 각 줄에 **원가 기여액**이 바로 보여 어디가 비싼지 즉시 파악된다.
- 단가가 빠진 품목은 그 줄에 빨간 표시 + 탭하면 그 품목 편집으로 바로 이동
  (지금은 "일부 재료 단가가 없어요" 문구만 뜨고 어느 것인지 찾아야 함).
- 기존 `.acc-row` 아코디언 패턴을 그대로 재사용 — 새 시각 언어를 만들지 않는다.
- **하위 트리를 펼치지 않는다** — 묵은반죽이 `manual`이므로 펼칠 하위가 애초에 없다(§3.3).

"+ 구성 추가"는 검색창 하나다. 구매 재료든 반죽이든 **같은 방식으로** 고르고,
없는 이름이면 그 자리에서 새 품목으로 등록된다(지금 `attachIngredientLinker()`의 "+ 새 재료로 추가"와 동일).

### 6.4 ★ 계산기 화면은 건드리지 않는다 (피드백 5번)

> "레시피랑 bom_lines 기반으로 전환하면 레시피 보기 난잡해질 거 같은데 괜찮음?"

**타당한 우려다. 그래서 전환 전략을 바꿨다.**

초판은 "계산기가 `bom_lines`를 직접 읽고 쓴다"였는데, 그러려면
계산기 렌더링·CSV 왕복·공유 RLS를 전부 다시 만져야 했다. 매일 쓰는 화면이라 위험이 크다.

**2판: 계산기는 지금 그대로 `recipes.flours`/`ingredients` jsonb를 쓴다.
저장할 때 `bom_lines`로 자동 미러링만 한다.**

```
레시피 저장 (saveCurrentRecipe)
   ↓
① 지금처럼 flours/ingredients jsonb 저장          ← 화면·코드 변화 없음
   ↓
② 각 재료 행의 이름을 normalizeForMatch()로 정규화  ← 기존 함수 재사용
   ↓
③ 같은 이름의 품목이 있으면 그 품목에 연결
   없으면 단가 0인 구매 품목을 자동 생성
   ↓
④ 이 레시피의 item에 bom_lines를 재생성
   (단, 사용자가 품목 화면에서 손으로 바꾼 연결은 이름이 같으면 유지 —
    기존 mergeSnapshotIngredientIds()와 같은 방식)
```

이 방식의 결과:

| | 효과 |
|---|---|
| 계산기 화면 | **변화 0** — 지금 보이는 그대로 |
| 사용자 추가 작업 | **0** — 저장만 하면 뒤에서 자동 연결 |
| 이중 입력(§2.5) | **사라짐** — 원가 화면에서 다시 연결할 필요 없음 |
| 스냅샷 수동 동기화(§2.4) | **사라짐** — 저장할 때마다 자동 갱신되므로 "다시 불러오기" 불필요 |
| 자동완성 | **넣지 않음** — 2026-07-24에 마찰로 걷어낸 것을 되살리지 않음 |

`recipes.flours`/`ingredients`가 원본이고 `bom_lines`가 파생이라 이중 저장이긴 하지만,
**동기화가 자동**이라는 점에서 지금의 `cost_snapshot_*`(수동)과 결정적으로 다르다.

### 6.5 공유/아카이브에는 하위 품목을 안 보여준다 (피드백 7번)

- 원가/단가는 **어느 경우에도 공유하지 않는다**(지금 `products`가 owner-only인 것과 같은 원칙).
- 공유받은 레시피는 지금처럼 `recipes.flours`/`ingredients`만 읽는다 —
  §6.4 덕에 계산기가 여전히 jsonb를 쓰므로 **공유 경로는 코드 변경이 아예 없다.**
- `items` / `bom_lines`는 owner-only RLS로 두면 끝이다. 하위 배합 노출 문제가 원천적으로 없다.

이 결정이 §6.4와 맞물려 **공유·아카이브·CSV를 전혀 건드리지 않아도 되게** 만든다 — 리뉴얼 범위가 크게 준다.

---

## 7. ★ 재료 가격 이력 추적 (피드백 3번, 신규)

> "CSV 불러오기 하면 기존에 있던 재료랑 새로 들어온 재료로 덮어지는 거 같은데,
> 나는 최신 자료를 보고 사용하고 싶고, 재료 가격이 어떻게 변화했는지 알고 싶음."

현재 `buildImportDiff()`는 확인 단계를 거치긴 하지만, 적용하면 `purchase_price`를 **덮어쓰고
이전 값은 사라진다.** 초판에서 "범위 밖"으로 뒀던 항목인데, 명시적 요구이므로 범위에 넣는다.

### 7.1 이력 테이블

```sql
create table item_price_history (
  id bigint generated always as identity primary key,
  item_id uuid not null references items(id) on delete cascade,
  purchase_amount numeric not null,
  purchase_unit text not null,
  purchase_price numeric not null,
  price_per_gram numeric not null,          -- 그 시점 환산 단가 (계산 결과 보존)
  effective_date date not null,             -- 거래명세서의 날짜 (없으면 가져온 날)
  source text not null default 'manual',    -- 'manual' | 'csv' | 'ledger'
  note text not null default '',
  created_at timestamptz not null default now()
);
create index item_price_history_item_idx on item_price_history(item_id, effective_date desc);
```

### 7.2 "최신 자료를 보고 쓴다"

- `items`의 현재 단가는 **`effective_date`가 가장 최근인 이력 행**과 항상 일치시킨다.
- 즉 사용자가 보는 원가는 언제나 최신 가격 기준이다 — 지금과 동작이 같다.
- 이력은 **덮어쓰는 대신 쌓기만** 한다.

### 7.3 거래명세서 CSV에서 이력을 자동으로 쌓는다

현재 `buildLedgerRows()`는 **같은 재료가 여러 날짜에 걸쳐 나오면 마지막 행만 남기고 버린다.**
이력 관점에서는 **버려지던 그 행들이 곧 가격 변화 데이터**다.

바뀌는 점:
1. 컬럼 매핑 화면(`startIngredientImportMapping()`)에 **날짜 칸 매핑 추가** (이름/단위/단가 + 날짜)
2. `buildLedgerRows()`가 중복 제거 대신 **모든 행을 (재료, 날짜, 가격)으로 반환**
3. 가져오기 미리보기는 지금처럼 유지하되, 표시를 이력 기준으로 바꾼다:
   ```
   버터   60,000원 (7/20)  →  100,000원 (7/25)   ▲ 66%
   ```
4. 적용하면 이력 행이 쌓이고, `items`의 현재 단가는 최신 날짜 행으로 갱신된다.

날짜 칸을 매핑하지 않은 CSV는 가져온 날짜를 `effective_date`로 쓴다(기존 동작과 호환).

### 7.4 가격 변화 보기

품목 상세에 "가격 변화" 영역을 둔다.

```
버터 (앵커버터)
현재  100,000원 / 10kg = 10.0원/g        (7/25 기준)

가격 변화
  7/25   100,000원   10.0원/g   ▲ 66%
  7/20    60,000원    6.0원/g
  7/02    58,000원    5.8원/g   ▲ 3%
```

- 최소 구현은 **표 하나**로 충분하다(그래프는 선택).
- 그래프를 그린다면 §11의 의존성 해제로 차트 라이브러리를 쓸 수 있다 —
  다만 **표만으로도 요구는 충족되므로 그래프는 후순위**로 둔다.

### 7.5 원가에 미치는 영향

- 원가 계산은 **항상 현재 단가(최신 이력)** 기준이다 — 과거 시점 원가 재현은 하지 않는다.
- 과거 원가 재현("7월 기준으로 계산해줘")은 이력이 쌓이면 나중에 얹을 수 있지만 이번 범위 밖이다.

---

## 8. 마이그레이션

기존 데이터는 **한 건도 잃지 않고** 자동 변환할 수 있다.

| 기존 | 이후 |
|---|---|
| `ingredients` 1행 | `items` 1행 (`kind='purchased'`, `cost_mode='manual'`) + `item_price_history` 1행(현재가) |
| `recipes` 1행 | `items` 1행 (`kind='made'`, `cost_mode='auto'`, `recipe_id`=원본) + `recipes` 행 유지 |
| `recipes.flours[]` / `ingredients[]` | `bom_lines` (`qty_unit='pct'`, `is_flour` 구분) |
| `products` 1행 | `items` 1행 (`is_sellable=true`) + `bom_lines` 1행(자식=그 레시피의 item, `qty`=`portion_weight`, `loss_pct`=`loss_rate_pct`) |
| `composite_products` 1행 | `items` 1행 (`is_sellable=true`) |
| `composite_product_components` 1행 | `bom_lines` 1행 |
| `cost_snapshot_*` 3개 컬럼 | **버림** (§6.4 자동 미러링이 대체) |

### 8.1 `ingredientId`가 null인 행 — 목록 폭발 주의

현재 상당수 행이 이름만 있고 연결이 안 된 상태다.
이름을 `normalizeForMatch()`로 정규화해 매칭하고, 안 되면 **단가 0인 구매 품목을 새로 만든다.**

**여기서 §10-2의 "목록 폭발" 위험이 발생한다.**
"물"/"정수"/"찬물"이 전부 별개 품목이 되는 식이다. 완화책:
- 마이그레이션 직후 **"비슷한 이름 병합 제안" 화면**을 한 번 띄운다
  (기존 `levenshtein()`/`findSimilarIngredient()` 재사용 — 이미 있는 함수다)
- 자동 생성된 단가 0 품목은 `category='미분류'`로 표시해 구분

### 8.2 안전장치

- 마이그레이션은 **한 번만** 실행되고, 기존 테이블은 **지우지 않는다**(읽기 전용으로 남김)
- 검증: 모든 레시피/제품의 원가를 신·구 엔진으로 각각 계산해 **오차 0.01원 이내인지 대조**
- 불일치 목록을 사용자에게 보여주고 확인받는다
- 구 테이블 삭제는 한참 뒤 별도 단계

---

## 9. 단계별 실행 계획

각 단계가 끝난 시점에 앱이 **정상 동작해야 한다.**

### 1단계 — 스키마 + 마이그레이션 (사용자 눈에 안 보임)
`items` / `bom_lines` / `item_price_history` 생성, RLS, 순환 트리거, 데이터 변환 + 신·구 원가 대조.
기존 화면은 계속 구 테이블 사용. 롤백 = 새 테이블 삭제.

### 2단계 — 롤업 엔진 (사용자 눈에 안 보임)
`rollupCost()` 구현 + 기존 3개 경로와 결과 대조. §5.1 소금빵 예시를 검증 케이스로.

### 3단계 — 품목 화면 신설
새 "품목" 서브페이지 + 필터 칩 + 품목 상세. **재료 관리 / 원가 계산은 아직 남겨둔다**(병행 기간).
이 시점부터 실사용 가치가 생긴다.

### 4단계 — 가격 이력 (§7)
CSV 날짜 매핑, 이력 쌓기, 품목 상세의 가격 변화 표.
**3단계와 독립적이라 순서를 바꿔도 된다** — 가격 이력이 더 급하면 먼저 해도 무방.

### 5단계 — 저장 시 자동 미러링 (§6.4)
`saveCurrentRecipe()`에 미러링 추가. 계산기 화면은 그대로.
이 시점에 이중 입력과 스냅샷 수동 동기화가 사라진다.

### 6단계 — 정리
구 화면 제거, `cost_snapshot_*` 컬럼 제거, 구 테이블 제거, 도움말 갱신.

**3단계까지만 해도 "제품/조합 제품 구분"과 "화면 3개" 문제는 풀린다.**
4·5단계는 각각 독립적 가치가 있으므로 반응을 보고 진행 여부를 정해도 된다.

---

## 10. ★ 전환 시 사용자가 혼란스러워할 요소 (피드백 1번, 신규)

> "BOM 원가계산 스펙으로 변경 시 유저가 혼란스러워하거나 불편할 요소가 존재할까?"

**존재한다. 7가지를 예상하며, 무시할 수 없는 것도 있다.**

### 10-1. "재료"가 "품목"으로 바뀌는 것 — 용어 이질감 ⚠️ 중
"재료 관리"는 제빵사에게 직관적인 말이다. "품목"은 ERP 용어라 낯설다.
- **완화**: 화면 이름을 "품목"이 아니라 **"재료"로 그대로 두는 것**을 권한다.
  내부 모델이 통합됐다는 걸 사용자가 알 필요는 없다.
  필터 칩이 "구매 재료 / 반죽 / 판매 제품"이면 굳이 상위 명칭을 바꾸지 않아도 말이 된다.

### 10-2. 목록이 갑자기 길어진다 ⚠️ 상
지금 재료 관리에는 밀가루·버터·소금만 있다. 마이그레이션 후엔 반죽과 제품까지 같은 목록에 들어온다.
게다가 §8.1 때문에 **이름만 다른 중복 품목이 대량 생성될 수 있다.**
- **완화**: (a) 필터 칩 기본값을 "구매 재료"로 → 첫인상은 지금과 동일
  (b) 마이그레이션 직후 "비슷한 이름 병합 제안" 화면 1회 제공
  (c) 자동 생성 품목은 `category='미분류'`로 묶어 한눈에 보이게

### 10-3. 내가 안 만든 항목이 생긴다 ⚠️ 중
레시피를 저장하면 품목이 자동 생성된다(§6.4). "내가 안 만들었는데 왜 있지?"
- **완화**: 자동 생성 품목에 표식을 달고, 품목 목록 상단에
  "레시피에서 자동으로 추가된 재료 N개 — 단가를 채워주세요" 안내를 둔다.
  이건 오히려 **할 일을 알려주는 장치**로 쓸 수 있다.

### 10-4. 삭제가 막힌다 ⚠️ 중
`on delete restrict` 때문에 "이 재료는 3개 배합에서 쓰이는 중이라 지울 수 없습니다"가 뜬다.
지금은 비활성화 후 자유롭게 완전 삭제가 됐다.
- **완화**: 거부 메시지에 **어느 배합에서 쓰이는지 목록을 같이 보여주고**,
  거기서 바로 이동할 수 있게 한다. 지금은 그런 정보 자체가 없어서
  (CLAUDE.md에도 "N개 레시피에서 사용 중" 경고를 의도적으로 안 만들었다고 기록돼 있다)
  **오히려 정보가 늘어나는 변화**다.

### 10-5. 새 입력칸(산출량, 제조 로스, 원가 산정 방식)이 늘어난다 ⚠️ 하
- **완화**: 전부 **선택 입력**이고 기본값이 지금 동작과 동일하다(§3.4).
  기본 화면에서는 접어두고, 필요한 사람만 펼치게 한다
  (태그 입력칸·사전발효반죽 카드에 이미 쓰는 "선택 항목은 접어두기" 패턴).

### 10-6. "단가 0"의 두 가지 의미가 섞인다 ⚠️ 중
묵은반죽에 의도적으로 0을 넣는 경우(§3.3)와, 아직 단가를 안 넣은 경우를 구분해야 한다.
구분 못 하면 "단가 없음" 경고가 계속 뜨거나, 반대로 진짜 누락을 놓친다.
- **완화**: `manual_cost_per_gram`을 **nullable**로 두고 `null`(미입력)과 `0`(0원으로 확정)을 구분한다.
  0을 명시적으로 입력하면 경고를 띄우지 않는다. ← **스키마에 반영 필요**

### 10-7. 병행 기간의 이중 화면 ⚠️ 중
3~5단계 동안 "재료 관리 / 원가 계산 / 품목"이 동시에 존재한다.
어느 쪽에서 고쳐야 하는지 헷갈릴 수 있고, 한쪽에서 고친 게 다른 쪽에 안 보이면 버그로 보인다.
- **완화**: 병행 기간을 **짧게** 가져가고, 구 화면 상단에
  "이 화면은 곧 [품목]으로 통합됩니다" 안내를 띄운다.
  또는 3단계에서 구 화면을 **즉시 읽기 전용으로** 만들어 편집 지점을 하나로 유지한다. ← 권장

### 종합 판단

**가장 큰 위험은 10-2(목록 폭발)이고, 나머지는 안내와 기본값으로 대부분 흡수된다.**
§6.4 결정(계산기 무변경) 덕에 **매일 쓰는 화면의 위험은 사실상 0**이 됐다 —
초판 대비 전환 리스크가 크게 줄었다.

---

## 11. 외부 의존성 방침 해제 (피드백 2번)

> "외부 의존성 없음이라는 기존 방침을 취소하자."

### 11-1. 적용 범위 — `account.html`만 해제하는 것을 권한다

원래 방침의 근거는 "베이커리 생산 라인의 불안정한 wifi"였다. 이걸 파일별로 따져보면:

| 파일 | 오프라인 동작 | 판단 |
|---|---|---|
| `account.html` | **불가** (로그인·DB가 전제) | 근거가 이미 무효 — **해제** |
| `index.html` / `manager.html` | **가능** (순수 계산기) | 근거가 그대로 유효 — **유지 권장** |

`account.html`은 이미 Supabase CDN과 AdSense를 쓰고 있어 사실상 방침이 깨져 있었다.
반면 `index.html`/`manager.html`은 생산 현장에서 오프라인으로 쓰라고 만든 파일이라,
여기에 CDN을 넣으면 **실제로 손해**다.

→ 별도 지시가 없으면 **해제 범위를 `account.html`로 한정**해 진행한다.

### 11-2. 그래서 무엇을 쓸 것인가 — "쓸 수 있다"와 "써야 한다"는 다르다

방침이 풀렸다고 라이브러리를 늘릴 이유는 없다. 이번 리뉴얼에서 실제로 값을 하는 후보만:

| 후보 | 용도 | 판단 |
|---|---|---|
| **SheetJS (xlsx)** | 거래명세서 `.xlsx` 직접 읽기 | **가치 있음** — §7.3과 직결. 지금은 사용자가 CSV로 변환해야 함 |
| 차트 라이브러리 | 가격 변화 그래프 (§7.4) | **후순위** — 표만으로 요구 충족. 정말 필요하면 인라인 SVG로도 가능 |
| BOM/그래프 라이브러리 | 롤업 계산 | **불필요** — 재귀 함수 하나면 된다 |
| 빌드 도구(Vite 등) | 파일 분리 | **보류** — 배포가 "파일 하나 복사"라 절차가 복잡해진다. `<script type="module">`로 의존성 없이 분리 가능 |

**SRI 원칙은 유지한다.** 지금 Supabase가 정확한 버전 + `integrity` 해시로 고정돼 있는데,
새로 넣는 것도 같은 방식으로 한다(버전 올릴 때 해시 재계산 필수 — 안 하면 로드가 아예 실패).

### 11-3. CLAUDE.md 갱신 필요

`Design constraints` 섹션의 "No external dependencies" 문구를 위 결정에 맞게 고쳐야 한다.
**실제 라이브러리를 처음 추가하는 시점에** 함께 고친다(지금은 코드 변경이 없으므로 스펙에만 기록).

---

## 12. `'ea'`(개수) 단위 — 확정 (피드백 8번 → 재확인)

> "ea 단위는 생각을 더 해봐야 할 듯. 계란 갯수로 제품을 만드는 경우도 있음."
> → "계란 구매기준과 사용기준이 다른데 둘 다 받는 게 맞을 것 같음" **(확정)**

### 12-1. 문제 — 원가는 전부 "1g에 얼마"로 계산된다

배합이 그램 기준이므로 모든 원가는 결국 `원/g`으로 환산돼야 한다.
그런데 계란을 판으로 사면 **"1개에 300원"까지만 나오고 "1g에 얼마"에서 막힌다.**
그래서 지금 `computePricePerGram()`(`account.html:2886`)은 `'ea'`면 그냥 `0`을 반환하고,
계란값이 원가에서 통째로 빠진다.

막힌 지점을 잇는 값이 **"1개가 몇 그램인가"** 하나다.

### 12-2. ★ 계란은 무게가 두 개다 — 둘 다 받는다 (확정)

| | 무게 | 의미 |
|---|---|---|
| **구매 기준** | 60g | 껍질 포함, 실제로 돈을 낸 무게 |
| **사용 기준** | 50g | 껍질 까고 배합에 들어간 무게 |

**나눌 때는 반드시 "사용 기준"으로 나눠야 한다:**

```
실사용 1g당 원가 = 300원 ÷ 50g = 6원/g          ← 맞음. 껍질값이 원가에 녹아든다
                  300원 ÷ 60g = 5원/g          ← 틀림. 껍질값 17%가 원가에서 증발
```

껍질값도 지불한 돈이므로, 실제로 배합에 들어간 50g이 그 300원 전체를 짊어져야 맞다.
(식품 원가에서 말하는 **가식부율/수율** 개념 — 여기서는 50/60 = 83%)

### 12-3. 스키마

```sql
-- items 테이블 (§4.1)
unit_weight_g   numeric,   -- 1개 무게 (구매 기준, 껍질 포함) — 계란이면 60
usable_weight_g numeric,   -- 실제 쓰는 무게 (손질 후) — 계란이면 50
                           -- null이면 unit_weight_g와 같다고 본다 (손질 로스 없는 재료)
```

두 칸 모두 **선택 입력**으로 둔다(§12-5 근거).

### 12-4. 계산 — 개수로도 그램으로도 쓸 수 있다

```
계란: 구매 30개 9,000원 / unit_weight_g = 60 / usable_weight_g = 50
  → 개당 구매가 = 9,000 ÷ 30 = 300원/개
  → 실사용 단가 = 300 ÷ 50  = 6원/g
```

| 배합에 이렇게 쓰면 | 환산 | 원가 |
|---|---|---|
| 계란 3개 (`qty_unit='ea'`) | 3 × 50g = 150g | 3 × 300 = **900원** |
| 계란 150g (`qty_unit='g'`) | 150 ÷ 50 = 3개분 | **900원** |
| 계란 10% (밀가루 1000g, `qty_unit='pct'`) | 100g = 2개분 | **600원** |

세 경로가 같은 논리로 일치한다.

**무게 단위로 구매하는 경우**(계란 1.8kg 9,000원)도 두 값이 있으면 처리된다 —
1,800g ÷ 60g = 30개 → 개당 300원 → 사용 기준 6원/g. 구매 기준 무게가 여기서 쓰인다.

### 12-5. 안 넣으면 어떻게 되는가 (필수 아님 — 권고)

무게를 **필수로 요구하지 않는다.** 입력 부담을 늘리는 것보다, 못 하는 일을 명확히 알려주는 쪽이 낫다.

| 상태 | 쓸 수 있는 라인 단위 |
|---|---|
| 무게 둘 다 없음 | `ea`만 — `g`/`pct` 라인에 넣으면 "1개 무게를 입력하면 그램으로도 쓸 수 있어요" 안내 |
| `unit_weight_g`만 있음 | 전부 (`usable_weight_g`는 같은 값으로 간주 — 손질 로스 없는 재료) |
| 둘 다 있음 | 전부 + 손질 로스가 원가에 반영됨 |

지금처럼 **조용히 0원이 되는 일은 없어진다** — 이것이 이 변경의 핵심 이득이다.

### 12-6. UI

```
계란
  구매    [ 30 ] 개      [ 9,000 ] 원
  1개 무게        [ 60 ] g     ← 사 온 그대로 저울에 올린 무게
  실제 쓰는 무게   [ 50 ] g     ← 손질 후. 같으면 비워두세요

  → 개당 300원 · 1g당 6원 (손질 로스 17%)
```

- 마지막 요약 줄이 **입력이 제대로 됐는지 즉시 확인시켜 준다**
  (지금 재료 관리의 "N원/g" 표시와 같은 역할).
- 슬라이스 치즈처럼 버릴 게 없는 재료는 "실제 쓰는 무게"를 비워두면 된다.
- 두 무게 칸은 `purchase_unit`이 `'ea'`일 때만 보여도 되지만,
  무게로 사면서 손질 로스가 있는 재료(통마늘 등)도 있으므로 **항상 보이되 접어두는** 쪽을 권한다.

### 12-7. 범위 밖 — 완제품을 개수로 세는 것 (확정)

"소금빵 100개 생산" 같은 것은 BOM이 아니라 **생산 계획** 영역이다.
사용자 확인 완료 — 이번 범위 밖(§13).

---

## 13. 범위 밖 (이번에 하지 않을 것)

- **생산 계획 / 발주** — BOM이 갖춰지면 자연스럽게 얹을 수 있지만(BOM 전개 = 소요량 계산), 이번은 원가까지.
- **재고 관리** — 입출고, 현재고.
- **과거 시점 원가 재현** — 이력은 쌓지만(§7), "7월 기준 원가"를 다시 계산하는 기능은 안 만든다.
- **원가 확정(freeze) / 버전 관리** — 필요해지면 이력 테이블 위에 얹으면 된다.
- **`index.html` / `manager.html`** — 로그인·DB 개념 자체가 없다. 전혀 건드리지 않는다.
- **재귀 중첩을 적극 활용하는 UI** — 구조상 가능하되 UI로 권하지 않는다(§3.3).

---

## 14. 요약

| | 현재 | BOM 이후 |
|---|---|---|
| 개념 수 | 4 (재료/레시피/제품/조합제품) | 1 (품목) + 1 관계 (BOM 라인) |
| 화면 수 | 3 | 1 (필터 칩으로 구분) |
| 제품 vs 조합 제품 | 별개 화면, 전환 불가 | 구분 없음 |
| 원가 계산 코드 | 3벌 (버그의 원인) | 1벌 (`rollupCost`) |
| 재료 연결 | 이중 입력 | 저장 시 자동 |
| 스냅샷 동기화 | 수동 "다시 불러오기" | 자동 미러링 |
| 단가 없음 안내 | "일부 재료가…" | 품목명으로 지목 |
| 가격 변화 추적 | **불가** (덮어씀) | 이력 테이블 + 변화 표 |
| `'ea'` 단위 원가 | 항상 0 | 1개 무게 + 손질 후 무게로 해결 (손질 로스까지 반영) |
| 하위 배합 | 표현 불가 | 수동 단가로 처리 (재귀는 옵션) |
| **계산기 화면** | — | **변화 없음** |

**초판과의 가장 큰 차이**: 초판은 "재귀 중첩을 지원하려고 계산기까지 갈아엎는" 설계였다.
2판은 **중첩을 빼고 계산기를 그대로 두면서도** 개념·화면·계산 로직의 중복이라는
실제 불편의 핵심을 해결한다. 전환 리스크는 크게 낮아지고, 얻는 것은 거의 그대로다.
